# Task: chore-syerp-service-split (D-P10-4 prerequisite)

Branch: `chore-syerp-service-split` off `feature-syerp-financial-reports` tip (code-identical to
tag `zj/good-09c-ap-aging-financial-statements`, plus the Phase-10 planning docs).
Goal: split `backend/app/modules/syerp/service.py` (3,824 lines, BACKLOG p2) into cohesive
submodules behind **unchanged public functions** (zero behavior change), and refresh
`.zj/codebase/MAP.md` (stale at migration 0009; head is 0011). Pure structural refactor.

Public surface preserved: all `from app.modules.syerp.service import (...)` call sites in
`router.py` and `backend/scripts/verify_*.py` keep working via package `__init__.py`
re-exports (incl. the dynamic `service._gl_account_id_by_code` reference). Split generated by
an AST script (verbatim function bodies) — no logic hand-transcribed.

Package layout (`service/`): `_common` (`_COST_QUANTUM`), `partners`, `locations`, `accounts`
(`list_gl_accounts`, `_gl_account_id_by_code`), `items`, `inventory` (on-hand/txns, moving-avg,
`post_receipt`, adjustments, transfers), `journal` (JE post/reverse/list/get, balances, register),
`purchasing` (POs, lines, receiving), `bills` (bills + payments), `reports`. Dependency graph
acyclic: leaves (`partners`/`locations`/`accounts`/`items`) → `inventory` → `journal` →
`purchasing` → `bills`; `reports` → `accounts`.

## Checklist
- [x] 1. Generate `service/` package via AST split (verbatim bodies); delete `service.py`; wire cross-imports
- [x] 2. Import parity: all submodules + package import clean; every router/verify name re-exported (93 top-level names identical; 104 pkg exports; router + privates all present)
- [x] 3. Regression green: all 11 verify scripts exit 0 (`verify_gl/ap/purchasing/inventory/e2e_p8/reports` + 3 `*_api` + 2 plum)
- [x] 4. Refresh `.zj/codebase/MAP.md` (migrations through 0011; service package layout)
- [ ] 5. Commit atomically on the chore branch

## Notes / deviation
- `verify_gl.py` (f) atomicity-rollback scenario retargeted its monkeypatch from
  `gl_service._gl_account_id_by_code` to `gl_service.purchasing._gl_account_id_by_code`:
  `receive_line` now resolves that name from the `purchasing` submodule's globals, so patching
  the package attribute no longer affects the call. Same implementation detail, new location —
  no assertion weakened. All 4 rollback assertions PASS again.
- MAP.md fuller refresh (Phases 8–9c, beyond migrations + service package) remains owed to
  `/zj:docs` — pre-existing, out of this chore's scope.
