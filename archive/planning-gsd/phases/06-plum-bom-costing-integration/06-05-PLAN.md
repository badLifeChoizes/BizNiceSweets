---
phase: 06-plum-bom-costing-integration
plan: 05
type: execute
wave: 5
depends_on: ["06-03", "06-04"]
files_modified:
  - frontend/src/routes/plum/PartDetail.tsx
  - frontend/src/routes/plum/ImportExport.tsx
  - frontend/src/routes/plum/ImportExport.test.tsx
  - frontend/src/routes/plum/components/PlumNav.tsx
  - frontend/src/App.tsx
  - docs/features/requirements-progress.md
autonomous: false
requirements: [PLUM-04, PLUM-05, PLUM-06, PLUM-07, PLUM-08, PLUM-09, PLUM-10]
user_setup: []

must_haves:
  truths:
    - "PartDetail shows four new cards below revision history: Bill of Materials, Approved Vendor List, Cost & Margin, Where Used"
    - "User can add a child part to the BOM, see it in the tree, switch to flat view, and remove it (Draft only)"
    - "User can link a vendor, edit price breaks, and select a price break for costing"
    - "Cost & Margin shows material cost, BOM roll-up, effective cost (with source), sale price, margin and margin %; Released shows frozen + live cost"
    - "Where Used lists direct and indirect parent assemblies"
    - "Import/Export page exports JSON and Excel and runs the 3-step import (upload → preview → commit)"
    - "PlumNav has an Import / Export tab and /plum/import-export route resolves"
  artifacts:
    - path: "frontend/src/routes/plum/PartDetail.tsx"
      provides: "Four new section cards wired to BomTree, AvlLinkSheet, cost/margin, where-used"
      contains: "Bill of Materials"
    - path: "frontend/src/routes/plum/ImportExport.tsx"
      provides: "Export buttons + 3-step import flow"
      contains: "Confirm Import"
    - path: "frontend/src/routes/plum/components/PlumNav.tsx"
      provides: "Import / Export nav tab"
      contains: "import-export"
    - path: "frontend/src/App.tsx"
      provides: "/plum/import-export route"
      contains: "import-export"
  key_links:
    - from: "frontend/src/routes/plum/PartDetail.tsx"
      to: "BomTree / AvlLinkSheet / cost mutation"
      via: "four section cards"
      pattern: "BomTree"
    - from: "frontend/src/routes/plum/ImportExport.tsx"
      to: "/api/v1/plum/import/preview"
      via: "upload-and-preview step"
      pattern: "import/preview"
---

<objective>
Integrate the Phase-6 frontend: insert the four section cards (BOM Tree, AVL, Cost & Margin, Where-Used) into PartDetail, build the Import/Export page with the 3-step import flow, add the PlumNav tab and App.tsx route, update requirements-progress.md, and pause at a human-verify checkpoint covering the seven success criteria.

Purpose: This is the user-facing surface that makes PLUM-04..10 real and verifiable end to end.
Output: extended PartDetail.tsx, ImportExport.tsx + test, PlumNav tab, App.tsx route, requirements-progress.md, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plum-bom-costing-integration/06-CONTEXT.md
@.planning/phases/06-plum-bom-costing-integration/06-PATTERNS.md
@.planning/phases/06-plum-bom-costing-integration/06-UI-SPEC.md
@.planning/phases/06-plum-bom-costing-integration/06-VALIDATION.md
@.planning/phases/06-plum-bom-costing-integration/06-03-SUMMARY.md
@.planning/phases/06-plum-bom-costing-integration/06-04-SUMMARY.md

<interfaces>
Components from Plan 04: BomTree (props partId, revisionId, revisionStatus, viewMode, onEdit, onRemove), BomLineSheet, AvlLinkSheet, PriceBreakEditor. Tooltip primitive installed.
API contracts (Plans 02-03): /parts/{id}/bom, /bom/flat, /where-used, /avl, /revisions/{revId}/cost; /export/json, /export/excel, /import/preview, /import/commit.
PartDetail existing structure (frontend/src/routes/plum/PartDetail.tsx): card header pattern lines 232-306; revision history ends ~line 371 (insert four cards after it); advanceMutation pattern lines 156-175 (template for saveCostMutation); STATUS_BADGE_CLASSES lines 60-65.
PlumNav TABS array (frontend/src/routes/plum/components/PlumNav.tsx lines 14-17). App.tsx route idiom line 43.
ImportPreviewResponse { new_count, updated_count, errors:[{row, field, message}] }.
Currency: read locale.currency from GET /api/v1/core/settings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PartDetail — four section cards (BOM, AVL, Cost & Margin, Where-Used)</name>
  <files>frontend/src/routes/plum/PartDetail.tsx</files>
  <read_first>
    - frontend/src/routes/plum/PartDetail.tsx (the file being modified — card header pattern lines 232-306, revision-history end ~line 371, advanceMutation lines 156-175, STATUS_BADGE_CLASSES lines 60-65)
    - frontend/src/routes/plum/components/BomTree.tsx (Plan 04 — props it accepts)
    - frontend/src/routes/plum/components/BomLineSheet.tsx and AvlLinkSheet.tsx (Plan 04 — open/close props)
    - frontend/src/routes/syerp/components/PartnerSheet.tsx (settings currency idiom lines 139-149)
    - .planning/phases/06-plum-bom-costing-integration/06-UI-SPEC.md (Section 1-4: card order fixed BOM→AVL→Cost&Margin→Where-Used; BOM card "Add Part" Draft-only + inline remove confirm; AVL card "Add Vendor" + expandable price-break rows + Select-for-Costing CheckCircle/Circle + D-14 dual-cost; Cost & Margin grid + inline edit form Draft-only + margin box + negative-margin destructive + currency suffix; Where-Used flat list direct/indirect + >20 notice; all empty-state and toast copy)
    - .planning/phases/06-plum-bom-costing-integration/06-CONTEXT.md (D-07 surface both entered+rollup, D-09 margin ungated, D-14 frozen+live)
  </read_first>
  <action>
    Extend PartDetail.tsx by inserting four `<Card>` sections (fixed order) after the revision-history div (~line 371), each using the card-header pattern from lines 232-306. (1) Bill of Materials — header "Bill of Materials" + "Add Part" button (variant outline size sm) shown only when current revision status==='draft'; body renders `<BomTree>` with the Tree|Flat toggle; wires BomLineSheet open state for add/edit and the inline "Remove {child}?" [Yes, Remove]/[Keep {child}] confirmation (Draft only) per UI-SPEC; remove calls DELETE /parts/{id}/bom/{lineId} and invalidates ['plum','parts',partId]. (2) Approved Vendor List — header "Approved Vendor List" + "Add Vendor"; body lists AVL links as expandable rows (Chevron toggle) with Preferred badge (bg-blue-50), three-dot DropdownMenu (Edit Vendor Link / Remove Vendor Link), nested price-break Table with the Select-for-Costing CheckCircle(green)/Circle indicator (clicking sets selected_vendor_link_id + selected_price_break_index on the Draft revision via PATCH cost endpoint; read-only on Released), and the D-14 dual-cost note ("Released at: $X · Current: $Y") on Released; remove uses a confirmation Dialog ("Remove vendor link?" / "Keep Link" / "Remove Link"); opens AvlLinkSheet for add/edit. (3) Cost & Margin — header "Cost & Margin"; two-column grid Material Cost / BOM Roll-up Cost / Effective Cost (font-semibold + source label) / Sale Price / Margin / Margin % (surface both entered and roll-up per D-07); inline always-visible edit form on Draft (Material Cost + Sale Price number inputs w-36 font-mono + "Save Costs" outline button → saveCostMutation PATCH /parts/{id}/revisions/{revId}/cost, toast "Costs saved."); margin box below with negative margin in text-destructive; Released shows frozen + live (D-14); currency code suffix from settings. (4) Where Used — header "Where Used"; flat `<ul aria-label="Where used">` list of parent assemblies with "Direct parent"/"Indirect via {parent}" labels sorted direct-first; >20 results shows the extensive notice; empty "This part is not used in any assembly." Add useQuery blocks for bom/avl/where-used with the ['plum','parts',partId,...] keys and saveCostMutation modeled on advanceMutation. All copy/markup is in 06-UI-SPEC.md Sections 1-4.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && grep -c "Bill of Materials" src/routes/plum/PartDetail.tsx && grep -c "Approved Vendor List" src/routes/plum/PartDetail.tsx && grep -c "Cost & Margin" src/routes/plum/PartDetail.tsx && grep -c "Where Used" src/routes/plum/PartDetail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` passes.
    - PartDetail contains all four card headers: "Bill of Materials", "Approved Vendor List", "Cost & Margin", "Where Used" (each grep returns >= 1).
    - saveCostMutation targets /api/v1/plum/parts/{partId}/revisions/{revId}/cost and toasts "Costs saved.".
    - "Add Part" and the inline edit form render only when current revision status === 'draft' (gated on status).
  </acceptance_criteria>
  <done>PartDetail shows the four section cards wired to BomTree/AvlLinkSheet/cost mutation/where-used with Draft gating and D-14 dual-cost; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: ImportExport page + PlumNav tab + App.tsx route + smoke test</name>
  <files>frontend/src/routes/plum/ImportExport.tsx, frontend/src/routes/plum/ImportExport.test.tsx, frontend/src/routes/plum/components/PlumNav.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/routes/plum/PartsList.tsx (page wrapper p-8 space-y-6 + query idiom)
    - frontend/src/routes/plum/PartDetail.tsx (Card/CardHeader/CardContent structure)
    - frontend/src/routes/plum/components/PartSheet.tsx (getApiErrorMessage; Loader2 button states)
    - frontend/src/routes/plum/components/PlumNav.tsx (TABS array lines 14-17)
    - frontend/src/App.tsx (route idiom line 43)
    - frontend/src/routes/plum/PartsList.test.tsx (smoke-test boilerplate)
    - .planning/phases/06-plum-bom-costing-integration/06-UI-SPEC.md (Import/Export route: Export card two buttons JSON/Excel with Download/FileSpreadsheet icons + note; Import card 3-step inline flow: Step 1 dashed dropzone accept .json,.xlsx + "Upload and Preview"; Step 2 preview summary banner + error Table + "Confirm Import" disabled-on-errors + "Back to Upload"; Step 3 success CheckCircle + counts + "Import Another File"; all copy/toasts)
    - .planning/phases/06-plum-bom-costing-integration/06-PATTERNS.md (sections "ImportExport.tsx", "PlumNav.tsx — extend", App.tsx route addition; blob download + FormData upload idioms)
  </read_first>
  <action>
    Create ImportExport.tsx (page wrapper p-8 space-y-6 with `<PlumNav />`, h1 "Import / Export", subtitle "Move data in and out of PLUM."). Export Card: "Export as JSON" (Download icon) and "Export as Excel" (FileSpreadsheet icon) outline buttons that GET /api/v1/plum/export/json and /export/excel with responseType 'blob', trigger a browser download via createObjectURL + anchor click, toast "Export started — your download will begin shortly.", Loader2 while exporting; note below. Import Card: step state useState<'upload'|'preview'|'committed'>. Step 1 dashed dropzone (Upload icon, "Drop a JSON or Excel file here", "Choose File" button, hidden input accept=".json,.xlsx", selected-filename display with clear X) + "Upload and Preview" (default) → POST FormData to /import/preview, set preview, advance to 'preview'. Step 2 summary banner ({new} new / {updated} existing / {errors} errors destructive) + error `<Table aria-label="Import validation errors">` (Row|Field|Error Message, max-h-64 scroll) + "Confirm Import" (default, disabled when errors>0) → POST same file to /import/commit, advance to 'committed' + toast "Import complete. {new} inserted, {updated} updated." + "Back to Upload" ghost. Step 3 success CheckCircle(green) + "Import complete" + counts body "No records were deleted." + "Import Another File" resets. Use getApiErrorMessage for failures (upload/commit copy from UI-SPEC). Add "Import / Export" tab to PlumNav TABS ({ to:'/plum/import-export', label:'Import / Export' }). Add `<Route path="/plum/import-export" element={<ImportExport />} />` to App.tsx. Create ImportExport.test.tsx (PartsList.test.tsx boilerplate): asserts Step-1 upload zone and both export buttons render. Exact copy/markup in 06-UI-SPEC.md.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && grep -c "import-export" src/routes/plum/components/PlumNav.tsx && grep -c "import-export" src/App.tsx && npm test -- --run src/routes/plum/ImportExport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` passes.
    - PlumNav TABS includes an 'import-export' entry; App.tsx registers the /plum/import-export route.
    - ImportExport.tsx contains "Upload and Preview", "Confirm Import", "Export as JSON", "Export as Excel".
    - ImportExport.test.tsx passes: upload zone + both export buttons asserted.
  </acceptance_criteria>
  <done>Import/Export page with export buttons + 3-step import, PlumNav tab, and App route wired; smoke test green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update requirements-progress.md; run full frontend + backend suites</name>
  <files>docs/features/requirements-progress.md</files>
  <read_first>
    - docs/features/requirements-progress.md (the file being modified — read the existing PLUM-01/02/03 rows for the exact format/columns to mirror)
    - .planning/phases/06-plum-bom-costing-integration/06-VALIDATION.md (the test-to-requirement map to cite as evidence)
  </read_first>
  <action>
    Update docs/features/requirements-progress.md: mark PLUM-04, PLUM-05, PLUM-06, PLUM-07, PLUM-08, PLUM-09, PLUM-10 as complete (mirror the exact row format used for PLUM-01..03), referencing Phase 6 and the implementing plans/tests (test_bom, test_avl, test_costing, test_import_export, BomTree.test, ImportExport.test) as evidence. Do not edit CHANGELOG.md (generated). Then run the full backend and frontend suites as the phase gate.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/pytest tests/ -q; cd ../frontend && npm test -- --run; grep -c "PLUM-10" /home/zack/Projects/BizNiceSweets/docs/features/requirements-progress.md</automated>
  </verify>
  <acceptance_criteria>
    - PLUM-04 through PLUM-10 are recorded complete in requirements-progress.md (grep PLUM-10 returns >= 1 on a complete row).
    - Full backend suite passes with a DB or skips cleanly (no errors/failures).
    - Full frontend suite passes (BomTree + ImportExport smoke tests green).
  </acceptance_criteria>
  <done>requirements-progress.md records PLUM-04..10 complete; full backend + frontend suites green or clean-skip.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the seven Phase-6 success criteria</name>
  <files>none — verification only</files>
  <action>Pause for the user to run the stack and verify all seven ROADMAP success criteria plus Released-revision immutability, per the steps in how-to-verify. No code is written in this task.</action>
  <what-built>
    The complete Phase-6 PLUM BOM/Costing/Integration surface: PartDetail now has Bill of Materials (tree + flat), Approved Vendor List (with price breaks and select-for-costing), Cost & Margin (with effective-cost source and frozen/live cost), and Where-Used cards; plus the Import/Export page (JSON + Excel export, 3-step upsert import).
  </what-built>
  <how-to-verify>
    Run the stack (per the Phase-3/4/5 dev convention — Vite dev overlay at http://localhost:5173 against the running API; rebuild frontend/dist + image before production :8000 reflects new UI). Then verify the seven ROADMAP success criteria:
    1. Open a Draft part → BOM card → "Add Part" → search a child part, set a decimal quantity (e.g. 0.5), Save → child appears in the tree; expand/collapse works.
    2. Switch the BOM card to "Flat" → a shared sub-assembly shows ONE row with summed Total Qty; the Total BOM Cost footer shows.
    3. Where Used card on a child part → its parent assemblies appear, labeled Direct / Indirect.
    4. AVL card → "Add Vendor" → search a SYERP vendor, add a price break, Save → the link persists and shows; mark another vendor Preferred.
    5. Cost & Margin → enter Material Cost, Save Costs → Effective Cost shows with source "manual"; on an assembly with no manual cost, Effective Cost shows "roll-up". Select a vendor price break → source becomes "vendor price".
    6. Set a Sale Price → Margin and Margin % render; a sale price below cost shows margin in red.
    7. Import/Export page → "Export as JSON" and "Export as Excel" download files; re-import the JSON via upload → preview shows counts and 0 errors → Confirm Import → success state; re-importing does not delete existing data. Try a >10 MB file → rejected.
    Also confirm a Released revision's BOM and cost are read-only (no Add Part / no edit form) and show the frozen "Released at" cost.
  </how-to-verify>
  <verify><human-check>User confirms all seven success criteria and Released-revision immutability.</human-check></verify>
  <done>User types "approved" after verifying all seven success criteria.</done>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → import/export API | File upload/download crosses here; client requests must carry auth and respect server gates |
| UI Draft/Released gating | Client hides edit affordances on Released revisions; backend 422 / RBAC is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-21 | Spoofing | client-only Released read-only gating | accept | UI hides BOM/cost edits on Released; backend (Plans 02-03) enforces immutability — client gate is convenience only |
| T-06-22 | Information Disclosure | export download exposes cost data | mitigate | export requests authenticated; backend gates plum:read and audits plum.exported (Plan 03) |
| T-06-23 | Denial of Service | huge file selected for import | mitigate | backend rejects >10 MB with 413 (Plan 03); UI surfaces the error via getApiErrorMessage |
| T-06-24 | Tampering | import without seeing impact | mitigate | UI enforces preview-before-commit (D-18); "Confirm Import" disabled while errors > 0 |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes; full frontend suite green.
- `cd backend && .venv/bin/pytest tests/ -q` green or clean-skip.
- Human-verify checkpoint confirms all seven ROADMAP success criteria.
</verification>

<success_criteria>
- PartDetail four cards live (PLUM-04/05/06/07/08/09).
- Import/Export page with export + 3-step import (PLUM-10).
- PlumNav tab + App route; requirements-progress.md updated.
- Human-verify checkpoint covering the seven success criteria.
</success_criteria>

<output>
Create `.planning/phases/06-plum-bom-costing-integration/06-05-SUMMARY.md` when done.
</output>
