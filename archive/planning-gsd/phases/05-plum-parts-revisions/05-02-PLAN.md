---
phase: 05-plum-parts-revisions
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/app/modules/plum/service.py
  - backend/app/modules/plum/router.py
  - backend/app/modules/plum/__init__.py
autonomous: true
requirements: [PLUM-01, PLUM-02, PLUM-03]
must_haves:
  truths:
    - "POST /api/v1/plum/parts creates a part and auto-creates its first Draft revision (D-03)"
    - "Duplicate user-supplied part number returns 409; auto-generated collision retries once"
    - "GET /api/v1/plum/parts searches by part number + description and filters by current-revision status and active flag"
    - "POST advance to released auto-obsoletes the prior released revision and writes both audit events (D-08, D-10)"
    - "Editing a Released revision returns 422 (server-enforced immutability, D-07)"
    - "All read endpoints require plum:read; all write endpoints require plum:write"
  artifacts:
    - path: "backend/app/modules/plum/service.py"
      provides: "list_parts, create_part, get_part, get_part_with_revisions, update_part, create_revision, advance_revision_status, generate_part_number, revision-label generation, VALID_TRANSITIONS"
      contains: "VALID_TRANSITIONS"
    - path: "backend/app/modules/plum/router.py"
      provides: "GET/POST /plum/parts, GET/PATCH /plum/parts/{id}, POST /plum/parts/{id}/revisions, POST /plum/parts/{id}/revisions/{rev_id}/advance, GET /plum/parts/next-number"
      contains: "require_permission"
  key_links:
    - from: "backend/app/modules/plum/router.py"
      to: "require_permission"
      via: "plum:read / plum:write dependency on every endpoint"
      pattern: "require_permission\\(\"plum:(read|write)\"\\)"
    - from: "backend/app/modules/plum/router.py"
      to: "write_audit"
      via: "audit on every mutation"
      pattern: "write_audit"
    - from: "backend/app/modules/plum/service.py"
      to: "VALID_TRANSITIONS"
      via: "FSM transition guard"
      pattern: "VALID_TRANSITIONS"
---

<objective>
Implement the PLUM backend service and router: part CRUD with auto-generated unique part numbers, server-side search + current-revision status filter, the revision state machine (Draft→In Review→Released→Obsolete) with supersede-on-release, server-enforced Released immutability, RBAC gating, and audit logging. Green the Wave 0 tests from 05-01.

Purpose: This is the authoritative business-logic layer — the FSM, immutability, and supersede invariants MUST live here (frontend guards are convenience only). PLUM-01/02/03 are satisfied by this plan's endpoints.

Output: Complete `plum/service.py` + `plum/router.py`, the fleshed-out `__init__.py` router registration, and a GREEN `tests/plum/` suite (when a DB is available).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plum-parts-revisions/05-CONTEXT.md
@.planning/phases/05-plum-parts-revisions/05-RESEARCH.md
@.planning/phases/05-plum-parts-revisions/05-PATTERNS.md
@.planning/phases/05-plum-parts-revisions/05-01-SUMMARY.md

<interfaces>
<!-- Contracts created in 05-01 — build against these, do not redefine. -->
From backend/app/modules/plum/models.py (05-01):
- PlumPart(id, part_number, active, created_at, updated_at)
- PlumPartRevision(id, part_id, revision_number, revision_label, status, description, category, unit_of_measure, notes, reason_for_revision, created_at, released_at, obsoleted_at)
- PlumClassificationTag(id, name, sort_order, active); PlumPartTag(part_id, tag_id)
From backend/app/modules/plum/schemas.py (05-01):
- PartCreate (description required; part_number optional; category/unit_of_measure/notes/reason_for_revision optional; tag_ids list[int])
- PartUpdate (all Optional incl. active, tag_ids, revision-controlled fields)
- PartRead (id, part_number, active, tags, current_revision_label, current_revision_status, created_at, updated_at)
- RevisionCreate (source_revision_id optional, reason_for_revision); RevisionRead (id, part_id, revision_number, revision_label, status, snapshot fields, timestamps)
- PartDetailRead (PartRead fields + revisions: list[RevisionRead])

<!-- Shared dependencies — reuse, do not re-implement. -->
From backend/app/modules/auth/dependencies.py:
- require_permission("plum:read") / require_permission("plum:write") — FastAPI dependency returning current_user.
From backend/app/modules/auth/service.py:
- write_audit(db, actor_id, action, target_type, target_id, detail) — append-only audit writer.
From backend/app/core/db.py:
- get_db — async session dependency.
From backend/app/core/settings_model.py:
- Setting(key, value) — read plum.revision_scheme to choose ASME vs SemVer label generation.

<!-- Route prefix rule: router uses prefix="/plum"; mount_all() in registry.py adds /api/v1. Full paths are /api/v1/plum/... -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PLUM service layer — CRUD, search, part-number + revision-label generation</name>
  <files>backend/app/modules/plum/service.py</files>
  <behavior>
    - generate_part_number(): returns "P00001" when no parts exist; "P00002" after P00001 exists (DB MAX query on P% part numbers)
    - create_part(): inserts PlumPart + auto-creates first PlumPartRevision in status "draft" with revision_number=1 and revision_label from the scheme (ASME "A" or SemVer "0.1.0"); applies tag_ids as PlumPartTag rows; returns the part (D-03)
    - create_part() with a user-supplied duplicate part_number raises HTTPException 409; an auto-generated collision retries once
    - list_parts(): excludes active=false by default; q searches part_number OR current-revision description via ilike; status_filter matches the current (latest non-obsolete) revision status using the latest-revision subquery (RESEARCH Pattern 4 via revision_number MAX)
    - get_part_with_revisions(): returns the part plus all revisions ordered revision_number DESC (newest first)
    - update_part(): applies exclude_unset fields; revision-controlled field edits target the current Draft revision; raises 422 if the current revision is "released" (immutable, D-07)
    - next revision-label generation: ASME advances A→B skipping I,O,Q,S,X,Z; SemVer minor-bumps for a new Draft and major-bumps (zeroing rest) on release (D-04/D-05)
  </behavior>
  <read_first>
    - backend/app/modules/syerp/service.py (the service analog — generate_partner_code MAX-query pattern, create-with-IntegrityError-retry, list with active filter + ilike search, get-by-id-with-404, update with model_dump(exclude_unset=True))
    - backend/app/modules/plum/models.py (05-01 — the exact columns to query/set)
    - backend/app/modules/plum/schemas.py (05-01 — PartCreate/PartUpdate/RevisionCreate field names)
    - backend/app/core/settings_model.py (read plum.revision_scheme via select(Setting).where(Setting.key=="plum.revision_scheme"))
    - .planning/phases/05-plum-parts-revisions/05-RESEARCH.md (Pattern 3 FSM, Pattern 4 latest-revision subquery, Pattern 6 label generation, Pattern 7 part-number gen, Pitfalls 1/2/4/7)
    - .planning/phases/05-plum-parts-revisions/05-PATTERNS.md (service.py Pattern Assignment — exact function signatures and the net-new FSM/subquery callouts)
  </read_first>
  <action>
    Implement `backend/app/modules/plum/service.py` with async functions against the 05-01 models, using explicit `select(...)` queries (NO ORM relationship access — async MissingGreenlet pitfall).

    Define module-level constants: `ASME_SKIP = {"I","O","Q","S","X","Z"}`, the derived `ASME_LETTERS` list, and `VALID_TRANSITIONS = {"draft":["in_review"], "in_review":["released","draft"], "released":["obsolete"], "obsolete":[]}` (D-07).

    Functions: `generate_part_number(db)` (P##### MAX-query, mirrors generate_partner_code); `_get_revision_scheme(db)` (reads the plum.revision_scheme Setting, defaults "asme"); `_first_revision_label(scheme)` ("A" for asme, "0.1.0" for semver, D-04); `_next_draft_label(scheme, source_label)` and `_release_label(scheme, current_label)` (ASME letter advance skipping the reserved letters; SemVer minor/major bumps per D-05); `create_part(db, data)` (insert part with IntegrityError-retry-once for auto-gen / 409 for user-supplied dup, insert first revision_number=1 status="draft" copying description/category/unit_of_measure/notes/reason_for_revision from data, insert PlumPartTag rows from data.tag_ids, commit, return part); `list_parts(db, q, status_filter, include_archived)` (active filter default-on, ilike on part_number + the joined current-revision description, status_filter via the latest-revision-per-part subquery built on MAX(revision_number) per part); `get_part(db, part_id)` (404 if missing); `get_part_with_revisions(db, part_id)` (part + revisions ordered revision_number DESC, shaped for PartDetailRead); `update_part(db, part_id, data)` (model_dump(exclude_unset=True); part-level fields onto PlumPart; revision-controlled fields onto the current Draft revision; raise 422 if current revision status=="released").

    Each function that mutates returns the ORM object so the router can audit + serialize.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.modules.plum import service as s; assert s.VALID_TRANSITIONS['draft']==['in_review']; assert 'I' not in s.ASME_LETTERS and 'B' in s.ASME_LETTERS; print('OK', s.ASME_LETTERS[:3])"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/modules/plum/service.py` defines `VALID_TRANSITIONS` with the exact D-07 mapping (draft→[in_review], in_review→[released,draft], released→[obsolete], obsolete→[])
    - `ASME_LETTERS` excludes I, O, Q, S, X, Z and includes A, B, C, ... (verify command asserts this and exits 0)
    - service.py contains `async def create_part`, `async def list_parts`, `async def get_part`, `async def get_part_with_revisions`, `async def update_part`, `async def generate_part_number`
    - No `relationship(` and no implicit lazy-load access; queries use `select(`: `grep -c "select(" backend/app/modules/plum/service.py` → ≥4
    - update_part raises HTTPException 422 when the current revision status is "released" (grep for `422` and `released` co-located)
    - The verify command imports the service and asserts the FSM + ASME letter set successfully
  </acceptance_criteria>
  <done>The service layer implements part CRUD, search, current-revision status filter, part-number generation, and revision-label generation with the FSM transition table and Released-immutability guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revision FSM service + PLUM router with RBAC and audit</name>
  <files>backend/app/modules/plum/service.py, backend/app/modules/plum/router.py, backend/app/modules/plum/__init__.py</files>
  <behavior>
    - create_revision(part_id, source_revision_id?): copies forward description/category/unit_of_measure/notes from the source revision (default latest released, fallback latest overall — D-03), inserts a new Draft revision with revision_number = MAX+1 and the next label; writes revision.created audit
    - advance_revision_status(part_id, rev_id, target): rejects invalid transitions with 422 per VALID_TRANSITIONS; on →released, sets prior released revision to obsolete + obsoleted_at and writes revision.obsoleted, then sets this revision released + released_at, writes revision.released; on →in_review writes revision.submitted; on in_review→draft writes revision.rejected (D-08, D-10)
    - GET /api/v1/plum/parts requires plum:read; all mutations require plum:write; missing permission → 403
    - GET /api/v1/plum/parts/next-number returns the auto-generated next part number (prefill for the create sheet)
    - every mutation endpoint calls write_audit with the correct action and target
  </behavior>
  <read_first>
    - backend/app/modules/syerp/router.py (the router analog — APIRouter prefix pattern, read-endpoint permission gate, write-endpoint + audit, archive-aware PATCH was_active pattern)
    - backend/app/modules/syerp/__init__.py (registration pattern to confirm in plum/__init__.py)
    - backend/app/modules/auth/service.py (write_audit signature)
    - backend/app/modules/auth/dependencies.py (require_permission)
    - backend/app/modules/plum/service.py (Task 1 — functions this router calls; add create_revision + advance_revision_status here)
    - backend/app/modules/plum/schemas.py (05-01 — response_model types)
    - .planning/phases/05-plum-parts-revisions/05-PATTERNS.md (router.py Pattern Assignment — endpoint shapes; Shared Patterns RBAC + Audit Logging tables; minimum audit events D-10)
    - .planning/phases/05-plum-parts-revisions/05-RESEARCH.md (Pattern 3 advance_revision_status reference; Pitfall 3 transaction/flush ordering for supersede)
  </read_first>
  <action>
    Add to `service.py`: `get_revision(db, revision_id)` (404 if missing); `get_released_revision(db, part_id)` (the current released rev or None); `create_revision(db, part_id, data, actor_id)` (resolve source = data.source_revision_id or latest-released-or-latest, copy-forward the snapshot fields, revision_number = MAX(revision_number for part)+1, label via `_next_draft_label`, status "draft", insert, commit); `advance_revision_status(db, part_id, revision_id, target_status, actor_id)` — validate part_id match (404) and transition (422 via VALID_TRANSITIONS); when target=="released", load prior released, set it obsolete + obsoleted_at, `await db.flush()` (Pitfall 3 — flush before the second update inside one transaction so the partial unique index never sees two released rows), write revision.obsoleted, then set this revision released + released_at; commit; write the target-specific audit action.

    Create `backend/app/modules/plum/router.py`: `router = APIRouter(prefix="/plum", tags=["plum"])` (NO /api/v1 — mount_all adds it). Endpoints:
      - GET /parts (q, status, include_archived; require_permission("plum:read")) → list[PartRead]
      - GET /parts/next-number (require_permission("plum:read")) → {"part_number": ...}
      - POST /parts (require_permission("plum:write")) → PartRead, 201; write_audit "part.created"
      - GET /parts/{part_id} (require_permission("plum:read")) → PartDetailRead (part + revisions)
      - PATCH /parts/{part_id} (require_permission("plum:write")) → PartRead; archive-aware was_active comparison → audit "part.archived" vs "part.updated"
      - POST /parts/{part_id}/revisions (require_permission("plum:write")) → RevisionRead, 201; audit "revision.created"
      - POST /parts/{part_id}/revisions/{rev_id}/advance (require_permission("plum:write"), body {target_status}) → RevisionRead; audit handled inside the service per transition
    Ensure `plum/__init__.py` (from 05-01) imports this real router and calls registry.register (replace the placeholder router.py content if 05-01 left a stub).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/plum/ -x -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/modules/plum/router.py` defines `router = APIRouter(prefix="/plum"` and does NOT contain the string `/api/v1`
    - Every endpoint declares a `require_permission("plum:read")` or `require_permission("plum:write")` dependency: `grep -c "require_permission(\"plum:" backend/app/modules/plum/router.py` → ≥7
    - Router has GET /parts, GET /parts/next-number, POST /parts, GET /parts/{part_id}, PATCH /parts/{part_id}, POST /parts/{part_id}/revisions, POST /parts/{part_id}/revisions/{rev_id}/advance
    - `write_audit` is called on every mutation handler: `grep -c "write_audit" backend/app/modules/plum/router.py backend/app/modules/plum/service.py` total → ≥5 (incl. revision.released + revision.obsoleted in service)
    - advance_revision_status calls `await db.flush()` between the obsolete-prior and release-current updates (Pitfall 3)
    - `cd backend && python -m pytest tests/plum/ -x -q` exits 0 when a DB is available, OR all DB-dependent tests report `skipped` via skip_if_no_db (no errors/failures from import or collection)
    - `cd backend && python -c "import app.modules.plum"` exits 0 (module self-registers with router mounted)
  </acceptance_criteria>
  <done>The revision FSM, supersede-on-release, and all PLUM endpoints are implemented with RBAC gates and audit logging; the Wave 0 backend suite passes (or skips cleanly without a DB).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/plum/* | All request input (q, status, part/revision bodies) is untrusted and crosses into DB queries |
| frontend status guard → API | Frontend disables actions for convenience; the API is the real authz + immutability boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Elevation of Privilege | All write endpoints (create/update/archive/create-revision/advance) | mitigate | `require_permission("plum:write")` on every mutation; tests assert 403 without it (test_create_requires_write_permission) |
| T-05-05 | Information Disclosure | All read endpoints | mitigate | `require_permission("plum:read")` on every GET |
| T-05-06 | Tampering | Search `?q=` param | mitigate | Parameterized `.ilike()` only — no raw SQL string interpolation (mirrors syerp/service.py) |
| T-05-07 | Tampering | Released revision immutability | mitigate | Server-side 422 in update_part when current revision status=="released" (D-07); test_released_revision_immutable asserts it |
| T-05-08 | Tampering | Two-Released race on supersede | mitigate | Single transaction with `await db.flush()` between obsolete-prior and release-current (Pitfall 3) + the DB partial unique index from 05-01 |
| T-05-09 | Repudiation | Audit trail for releases/obsoletes | mitigate | write_audit (append-only) called for part.created, revision.released, revision.obsoleted at minimum (D-10) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/plum/ -x -q` passes (or skips cleanly without DB)
- `cd backend && python -m pytest` full backend suite stays green
- Router uses prefix="/plum" with no /api/v1; every endpoint RBAC-gated
- revision.released + revision.obsoleted audit events both fire on release
</verification>

<success_criteria>
- PLUM-01: part create (with auto first revision)/read/update/archive endpoints live
- PLUM-02: search by number+description, current-revision status filter, active/archived toggle
- PLUM-03: create-revision (copy-forward) + advance-status FSM with supersede-on-release
- Released revisions immutable server-side (422)
- RBAC + audit on all endpoints; Wave 0 backend suite green
</success_criteria>

<output>
Create `.planning/phases/05-plum-parts-revisions/05-02-SUMMARY.md` when done.
</output>
