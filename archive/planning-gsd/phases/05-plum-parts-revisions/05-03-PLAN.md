---
phase: 05-plum-parts-revisions
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - frontend/src/routes/plum/PartsList.tsx
  - frontend/src/routes/plum/components/PlumNav.tsx
  - frontend/src/routes/plum/components/PartSheet.tsx
  - frontend/src/routes/plum/components/ArchivePartDialog.tsx
  - frontend/src/routes/plum/PartsList.test.tsx
autonomous: true
requirements: [PLUM-01, PLUM-02]
must_haves:
  truths:
    - "The Parts list screen renders a table of parts with part number, tags, current revision label, and status badge"
    - "Search input (debounced 300ms) filters the list by part number or description via ?q="
    - "A status Select filters by current-revision status via ?status="
    - "A Show-archived Switch toggles ?include_archived="
    - "Create Part opens the PartSheet; saving creates a part and invalidates the list"
    - "Row actions offer Edit and Archive/Restore; clicking a row navigates to /plum/parts/:id"
  artifacts:
    - path: "frontend/src/routes/plum/PartsList.tsx"
      provides: "Parts list screen with toolbar, table, empty/error states"
      contains: "/api/v1/plum/parts"
    - path: "frontend/src/routes/plum/components/PlumNav.tsx"
      provides: "PLUM sub-nav tab strip"
      contains: "PLUM sections"
    - path: "frontend/src/routes/plum/components/PartSheet.tsx"
      provides: "Create/edit part sheet with auto-prefilled part number + tag picker"
      contains: "Create Part"
    - path: "frontend/src/routes/plum/components/ArchivePartDialog.tsx"
      provides: "Archive confirmation dialog"
      contains: "Archive part?"
    - path: "frontend/src/routes/plum/PartsList.test.tsx"
      provides: "Wave 0 frontend smoke test"
      contains: "describe"
  key_links:
    - from: "frontend/src/routes/plum/PartsList.tsx"
      to: "/api/v1/plum/parts"
      via: "TanStack useQuery on ['plum','parts',{q,status,includeArchived}]"
      pattern: "\\['plum', ?'parts'"
    - from: "frontend/src/routes/plum/PartsList.tsx"
      to: "/plum/parts/:id"
      via: "row onClick navigate"
      pattern: "navigate\\(`/plum/parts"
---

<objective>
Build the PLUM Parts List screen and its supporting components (PartSheet create/edit, ArchivePartDialog, PlumNav tab strip), mirroring the Vendors.tsx + PartnerSheet.tsx + PartnerArchiveDialog.tsx + SyerpNav.tsx analogs. Satisfies PLUM-01 (create/edit/archive UI) and PLUM-02 (search + status filter + archived toggle).

Purpose: Gives the user the list-level part management surface. Row click deep-links to the Part Detail route (built in 05-04). App.tsx route wiring is owned by 05-04 (which imports both PartsList and PartDetail) to avoid a file-overlap conflict.

Output: PartsList screen + three components + a Wave 0 frontend smoke test, all consuming the 05-02 API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plum-parts-revisions/05-CONTEXT.md
@.planning/phases/05-plum-parts-revisions/05-UI-SPEC.md
@.planning/phases/05-plum-parts-revisions/05-PATTERNS.md
@.planning/phases/05-plum-parts-revisions/05-02-SUMMARY.md

<interfaces>
<!-- API contract from 05-02 — call these endpoints. -->
- GET  /api/v1/plum/parts?q=&status=&include_archived=  -> PartRead[]
- GET  /api/v1/plum/parts/next-number                   -> { part_number: string }
- POST /api/v1/plum/parts                                -> PartRead (body: PartCreate)
- PATCH /api/v1/plum/parts/{id}                          -> PartRead (body: PartUpdate; {active:false} archives, {active:true} restores)
- GET  /api/v1/core/settings                             -> SettingRead[] (read plum.revision_scheme for label display)

<!-- PartRead shape (from 05-01 schemas, surfaced by 05-02): -->
interface PartRead { id: string; part_number: string; active: boolean; tags: string[]; current_revision_label?: string | null; current_revision_status?: string | null; created_at: string; updated_at: string }

<!-- Classification tag vocabulary: seeded names Purchased, Manufactured, Assembly, Finished Good, Tool, Raw Material. The create body uses tag_ids: number[]. Read the tag list from whichever GET 05-02-SUMMARY documents (tags endpoint or embedded). -->
</interfaces>

<frontend_analogs>
- frontend/src/routes/syerp/Vendors.tsx — list screen (imports, fetch fn, debounced search, useQuery, restore mutation, page wrapper, toolbar, table, empty/error states, DropdownMenu actions)
- frontend/src/routes/syerp/components/SyerpNav.tsx — tab strip (copy verbatim, replace syerp->plum, single "Parts" tab)
- frontend/src/routes/syerp/components/PartnerSheet.tsx — sheet sections + Separator, getApiErrorMessage helper, useEffect form population, settings-read query, create/edit mutations + invalidation
- frontend/src/routes/syerp/components/PartnerArchiveDialog.tsx — destructive dialog shell + archive mutation
- frontend/src/routes/syerp/Vendors.test.tsx — Wave 0 smoke test (mock @/api/client, QueryClientProvider + MemoryRouter render, heading + empty-state assertions)
</frontend_analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlumNav + ArchivePartDialog + PartSheet</name>
  <files>frontend/src/routes/plum/components/PlumNav.tsx, frontend/src/routes/plum/components/ArchivePartDialog.tsx, frontend/src/routes/plum/components/PartSheet.tsx</files>
  <read_first>
    - frontend/src/routes/syerp/components/SyerpNav.tsx (copy verbatim — adapt TABS to a single { to: '/plum/parts', label: 'Parts' }, aria-label "PLUM sections")
    - frontend/src/routes/syerp/components/PartnerArchiveDialog.tsx (copy the dialog shell + archive mutation; drop the partner role logic; PATCH {active:false})
    - frontend/src/routes/syerp/components/PartnerSheet.tsx (the sheet analog — getApiErrorMessage helper verbatim, Sheet + SheetHeader + Separator sections, useEffect form population, settings-read query, create/edit mutations + invalidate ['plum','parts'])
    - .planning/phases/05-plum-parts-revisions/05-UI-SPEC.md (PartSheet sections Identity/Classification/Revision-seed; ArchivePartDialog copy; Copywriting Contract; Accessibility Contracts)
    - .planning/phases/05-plum-parts-revisions/05-PATTERNS.md (PartSheet.tsx, ArchivePartDialog.tsx, PlumNav.tsx Pattern Assignments — exact JSX and the PartRead interface export location)
  </read_first>
  <action>
    Create `PlumNav.tsx` by copying `SyerpNav.tsx`: a single TABS entry `{ to: '/plum/parts', label: 'Parts' }`, `aria-label="PLUM sections"`, active tab `border-primary text-foreground`, leave a code comment for the Phase 6 BOMs tab.

    Create `ArchivePartDialog.tsx` from `PartnerArchiveDialog.tsx`: props `{ open, part: PartRead | null, onClose }`; archiveMutation PATCHes `/api/v1/plum/parts/${id}` with `{ active: false }`, on success invalidates `['plum','parts']` and toasts "Part archived."; DialogTitle "Archive part?"; DialogDescription using part_number + description per the UI-SPEC copy; footer "Keep Part" (outline) + "Archive Part" (destructive, aria-label `Archive {part_number}`); Loader2 while archiving.

    Create `PartSheet.tsx` from `PartnerSheet.tsx`: export the `PartRead` interface (single source of truth consumed by PartsList + ArchivePartDialog, mirroring the PartnerRead-in-PartnerSheet decision). Copy `getApiErrorMessage` verbatim. Three Separator-divided sections: (1) Identity — Part Number Input auto-prefilled from `GET /api/v1/plum/parts/next-number` on create (helper "System-generated. You may change it before saving."), Description Input (required); (2) Classification — a checkbox group over the seeded tag vocabulary writing tag_ids: number[] (zero-or-more, D-12); (3) create-mode-only Revision seed — optional "Reason for first revision" textarea. On create, POST `/api/v1/plum/parts` with `{ part_number?, description, category?, unit_of_measure?, notes?, reason_for_revision?, tag_ids }`; on edit, PATCH `/api/v1/plum/parts/{id}`. onSuccess invalidate `['plum','parts']`, toast "Part created." / "Part saved.", onClose. onError toast getApiErrorMessage. Footer Cancel (outline) + Save Part (default) with Loader2.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "plum/(PlumNav|ArchivePartDialog|PartSheet)" ; grep -q "export interface PartRead" frontend/src/routes/plum/components/PartSheet.tsx && grep -q "PLUM sections" frontend/src/routes/plum/components/PlumNav.tsx && grep -q "Archive part?" frontend/src/routes/plum/components/ArchivePartDialog.tsx && echo ARTIFACTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/plum/components/PlumNav.tsx` contains `aria-label="PLUM sections"` and a NavLink to `/plum/parts`
    - `frontend/src/routes/plum/components/ArchivePartDialog.tsx` contains "Archive part?" and PATCHes with `{ active: false }` and invalidates `['plum','parts']`
    - `frontend/src/routes/plum/components/PartSheet.tsx` contains `export interface PartRead`, the verbatim `getApiErrorMessage` helper, a Part Number input prefilled from `/api/v1/plum/parts/next-number`, a required Description input, and a tag checkbox group writing `tag_ids`
    - `cd frontend && npx tsc --noEmit` reports no errors in the three new files (the grep filter shows no plum/ errors; ARTIFACTS_OK prints)
    - PartSheet create mutation POSTs to `/api/v1/plum/parts` and invalidates `['plum','parts']`
  </acceptance_criteria>
  <done>PlumNav, ArchivePartDialog, and PartSheet exist, type-check, export the shared PartRead interface, and follow the UI-SPEC copy + accessibility contracts.</done>
</task>

<task type="auto">
  <name>Task 2: PartsList screen + Wave 0 smoke test</name>
  <files>frontend/src/routes/plum/PartsList.tsx, frontend/src/routes/plum/PartsList.test.tsx</files>
  <read_first>
    - frontend/src/routes/syerp/Vendors.tsx (the list-screen analog — imports, fetchParts fn, debounced-search state, useQuery, restore mutation, page wrapper, toolbar, table, empty/error states, DropdownMenu row actions)
    - frontend/src/routes/syerp/Vendors.test.tsx (the smoke-test analog — mock @/api/client, render helper with QueryClientProvider + router, heading + empty-state assertions)
    - frontend/src/routes/plum/components/PartSheet.tsx (Task 1 — imports PartRead + PartSheet)
    - frontend/src/routes/plum/components/ArchivePartDialog.tsx, frontend/src/routes/plum/components/PlumNav.tsx (Task 1 — imported into the list)
    - .planning/phases/05-plum-parts-revisions/05-UI-SPEC.md (Screen 1 Parts List — toolbar controls, table columns, status badge color map, row navigation, empty-state copy, Copywriting Contract)
    - .planning/phases/05-plum-parts-revisions/05-PATTERNS.md (PartsList.tsx Pattern Assignment — fetch fn, debounce, useQuery key, restore mutation, page wrapper, toolbar, row navigate, empty state, DropdownMenu)
  </read_first>
  <action>
    Create `PartsList.tsx` from `Vendors.tsx`. Import PlumNav, PartSheet (+ PartRead), ArchivePartDialog, useNavigate. `fetchParts(q, status, includeArchived)` builds URLSearchParams and GETs `/api/v1/plum/parts`. Debounced search (300ms useRef pattern). useQuery key `['plum','parts',{ q: searchFilter, status: statusFilter, includeArchived }]`. Page wrapper `p-8 space-y-6` with PlumNav, h1 "Parts", subtitle "Manage parts and their revision history." Toolbar (`flex items-center gap-4`): search Input (`max-w-xs`, placeholder "Search part number or description…"), status `<Select>` (All Statuses | Draft | In Review | Released | Obsolete → `?status=`), Show-archived `<Switch>`+`<Label>`, Create Part `<Button variant="default" className="ml-auto">`. Table columns: Part Number (`font-medium`, row click → `navigate(\`/plum/parts/${part.id}\`)`), Description, Tags (comma-joined or "—"), Current Revision (current_revision_label or "—"), Status (status badge using the UI-SPEC color map: draft gray, in_review yellow, released green, obsolete gray), Actions (DropdownMenu `h-11 w-11` ghost trigger with `e.stopPropagation()`; Edit → openEditSheet, Archive/Restore based on part.active). Empty states (no parts / search active / filter active) and error state ("Could not load parts. Check your connection and try again.") per the Copywriting Contract. Restore mutation PATCHes `{active:true}` and invalidates `['plum','parts']`.

    Create `PartsList.test.tsx` from `Vendors.test.tsx`: mock `@/api/client`, render PartsList inside QueryClientProvider + a router (MemoryRouter or equivalent so useNavigate works), and assert (1) the "Parts" heading renders and the "Create Part" button is present, (2) the "No parts yet" empty state renders when the API returns `[]`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/routes/plum/PartsList.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/plum/PartsList.tsx` GETs `/api/v1/plum/parts`, uses query key matching `['plum','parts'`, and contains `navigate(\`/plum/parts/${` for row click navigation
    - The toolbar has a debounced search input, a status `<Select>`, a Show-archived `<Switch>`, and a "Create Part" button
    - The status badge color map matches the UI-SPEC (draft/in_review/released/obsolete distinct classes)
    - `frontend/src/routes/plum/PartsList.test.tsx` contains `describe` and asserts the "Parts" heading and the "No parts yet" empty state
    - `cd frontend && npx vitest run src/routes/plum/PartsList.test.tsx` exits 0 (smoke test passes)
    - `cd frontend && npx tsc --noEmit` reports no errors in PartsList.tsx
  </acceptance_criteria>
  <done>The Parts list screen renders, searches, filters by status, toggles archived, opens the create/edit sheet, archives/restores, and navigates to Part Detail on row click; the Wave 0 smoke test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-rendered part data → DOM | Part description / tags from DB rendered in the table; untrusted string content |
| frontend action gating → API | DropdownMenu Edit/Archive availability is convenience; the API (05-02) is the real authz boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | XSS via part description / notes rendered in table + sheet | mitigate | React auto-escapes JSX interpolation; no `dangerouslySetInnerHTML` anywhere in the new components |
| T-05-11 | Information Disclosure | Archived parts surfaced by default | mitigate | List query defaults include_archived unset (false); Show-archived Switch is explicit opt-in (D-11) |
| T-05-SC | Tampering | npm installs | accept | No new packages — all shadcn components already installed (UI-SPEC: "No new shadcn installs required") |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/routes/plum/PartsList.test.tsx` passes
- `cd frontend && npx tsc --noEmit` clean for plum files
- PartsList consumes ['plum','parts'] query key and navigates to /plum/parts/:id on row click
- No dangerouslySetInnerHTML in any new component
</verification>

<success_criteria>
- PLUM-01: create/edit/archive/restore part UI via PartSheet + ArchivePartDialog + row actions
- PLUM-02: debounced search + status Select + Show-archived Switch wired to query params
- Status badge color map per UI-SPEC; row click deep-links to Part Detail
- Wave 0 frontend smoke test green
</success_criteria>

<output>
Create `.planning/phases/05-plum-parts-revisions/05-03-SUMMARY.md` when done.
</output>
