---
phase: 07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/plum/ImportExport.tsx
autonomous: true
requirements: [PLUM-10]
must_haves:
  truths:
    - "After a successful import commit, the Parts List reflects the imported data without a manual refresh"
  artifacts:
    - path: "frontend/src/routes/plum/ImportExport.tsx"
      provides: "Cache invalidation on import commit success"
      contains: "invalidateQueries"
  key_links:
    - from: "frontend/src/routes/plum/ImportExport.tsx commitImportMutation.onSuccess"
      to: "TanStack Query cache key ['plum','parts']"
      via: "queryClient.invalidateQueries"
      pattern: "invalidateQueries\\(\\{ queryKey: \\['plum', 'parts'\\] \\}\\)"
---

<objective>
Close the PLUM-10 cache-staleness gap: `ImportExport.tsx`'s `commitImportMutation` never invalidates the `['plum','parts']` query after a successful import, so the Parts List shows stale data for up to the global 30s `staleTime`. Apply the exact invalidation pattern already used by six sibling components in the same route tree — a missed application of an existing convention, not a new pattern.

Purpose: A user who imports data sees it in the Parts List immediately.
Output: `ImportExport.tsx` invalidates `['plum','parts']` on commit success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-RESEARCH.md
@.planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-PATTERNS.md

<interfaces>
<!-- The established invalidation convention this fix replicates. Do not invent a new one. -->

From frontend/src/routes/plum/components/ArchivePartDialog.tsx (working analog):
```typescript
import { useMutation, useQueryClient } from '@tanstack/react-query'
const queryClient = useQueryClient()
// inside onSuccess:
void queryClient.invalidateQueries({ queryKey: ['plum', 'parts'] })
```

Current ImportExport.tsx state:
- line 30: `import { useMutation } from '@tanstack/react-query'`   (useQueryClient NOT imported)
- line 103: `export function ImportExport() {`
- line 164: `const commitImportMutation = useMutation<ImportCommitResponse, Error, File>({`
- line 175: commitImportMutation `onSuccess: (data) => { setCommittedData(data); setImportStep('committed'); toast(...) }`  (no invalidation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invalidate ['plum','parts'] on import commit success</name>
  <files>frontend/src/routes/plum/ImportExport.tsx</files>
  <read_first>
    - frontend/src/routes/plum/ImportExport.tsx (full file — especially line 30 imports, line 103 component body, lines 164-185 commitImportMutation)
    - frontend/src/routes/plum/components/ArchivePartDialog.tsx (lines 16, 41-42, 49-53 — the exact analog: import, hook setup, onSuccess invalidation)
    - frontend/src/lib/queryClient.ts (confirm the 30s staleTime this fix works around; do NOT change it)
  </read_first>
  <action>
    Make three edits in `frontend/src/routes/plum/ImportExport.tsx`, matching the ArchivePartDialog analog verbatim: (1) widen the line-30 import to `import { useMutation, useQueryClient } from '@tanstack/react-query'`; (2) add `const queryClient = useQueryClient()` near the top of the `ImportExport()` component body (line ~103, alongside the existing useState/useRef declarations); (3) inside `commitImportMutation`'s `onSuccess` (line ~175), after the existing `setCommittedData` / `setImportStep('committed')` / `toast(...)` lines, add `void queryClient.invalidateQueries({ queryKey: ['plum', 'parts'] })`. Apply this to `commitImportMutation` ONLY — `exportJsonMutation`, `exportExcelMutation`, and `uploadPreviewMutation` (preview-only, no DB write) must NOT get invalidation, matching current correct behavior. Do NOT change the global `staleTime` in `frontend/src/lib/queryClient.ts` (that would mask the missing invalidation project-wide and defeat the deliberate self-hosted-noise-reduction design).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run test -- --run ImportExport</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useQueryClient" frontend/src/routes/plum/ImportExport.tsx` returns at least 2 (import + hook call)
    - ImportExport.tsx contains `invalidateQueries({ queryKey: ['plum', 'parts'] })` exactly once
    - The invalidation appears inside `commitImportMutation`'s onSuccess (grep-confirm it is NOT added to exportJson/exportExcel/uploadPreview mutations — `grep -c invalidateQueries` returns 1)
    - `cd frontend && npx tsc --noEmit` exits 0 (zero type errors)
    - `cd frontend && npm run test -- --run ImportExport` exits 0 (the 3 existing smoke tests still pass)
    - `frontend/src/lib/queryClient.ts` is unchanged (git diff empty)
  </acceptance_criteria>
  <done>ImportExport.tsx imports and uses useQueryClient, and invalidates the ['plum','parts'] key exactly once on commit success; tsc and the existing smoke tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client cache | Client-side TanStack Query cache only; no server-side trust boundary crossed by this change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information disclosure (stale data) | Parts List after import | mitigate | Adding the missing `invalidateQueries` forces a refetch so the UI reflects committed server state promptly; scoped to `commitImportMutation` only, no auth/permission logic touched. |

No new endpoints, no auth changes, no package installs — this plan introduces no new attack surface (no supply-chain checkpoint required).
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` → 0 errors
- `cd frontend && npm run test -- --run ImportExport` → existing smoke tests green
- Immediate-refresh behavior is confirmed interactively in the Plan 07-03 human-verify checkpoint (a live query-client refetch is not practically unit-testable without a harness the project does not have).
</verification>

<success_criteria>
- ImportExport.tsx invalidates `['plum','parts']` exactly once, on import-commit success only, using the existing project convention.
- Type check and existing frontend tests pass; global staleTime untouched.
</success_criteria>

<output>
Create `.planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-02-SUMMARY.md` when done.
</output>
