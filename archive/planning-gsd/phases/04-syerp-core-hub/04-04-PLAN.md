---
phase: 04-syerp-core-hub
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - frontend/src/routes/syerp/GLAccounts.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [SYERP-01, SYERP-02, SYERP-03, SYERP-04, SYERP-05]
must_haves:
  truths:
    - "The Chart of Accounts screen renders the seeded CoA as 5 read-only grouped cards (D-06:)"
    - "Vendor, Customer, and GL screens are reachable via routes under the AppShell"
    - "The SYERP nav entries appear for a permitted user and the screens function end to end"
  artifacts:
    - path: "frontend/src/routes/syerp/GLAccounts.tsx"
      provides: "Read-only chart-of-accounts browse screen"
      contains: "Chart of Accounts"
    - path: "frontend/src/App.tsx"
      provides: "Routes for /syerp/vendors, /syerp/customers, /syerp/gl under AppShell"
      contains: "syerp"
  key_links:
    - from: "frontend/src/routes/syerp/GLAccounts.tsx"
      to: "/api/v1/syerp/gl/accounts"
      via: "useQuery read-only fetch of seeded accounts"
      pattern: "gl/accounts"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/routes/syerp/Vendors.tsx"
      via: "Route element under the AppShell layout"
      pattern: "Vendors"
---

<objective>
Add the read-only Chart of Accounts browse screen, register all three SYERP
routes under the AppShell, and verify the full Phase 4 experience end to end via
a human-verify checkpoint.

Purpose: SYERP-05's "visible and browsable" criterion and the integration of the
Vendor/Customer/GL screens into the running app navigation (Phase 3 D-04: nav =
enabled module ∩ user permission). This plan owns App.tsx so it does not collide
with Plan 03.
Output: GLAccounts.tsx, App.tsx routes wired, and a verified working phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-syerp-core-hub/04-CONTEXT.md
@.planning/phases/04-syerp-core-hub/04-PATTERNS.md
@.planning/phases/04-syerp-core-hub/04-UI-SPEC.md
@.planning/phases/04-syerp-core-hub/04-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor consumes. -->

Backend API (Plan 02): GET /api/v1/syerp/gl/accounts -> GLAccountRead[]
  GLAccountRead: { id, code, name, account_type ('ASSET'|'LIABILITY'|'EQUITY'|'REVENUE'|'EXPENSE'),
  parent_id, active }. Returned flat, ordered by code; tree grouping is frontend-side.

From frontend/src/App.tsx (current): Routes with an AppShell layout route wrapping
  /, /settings, /settings/modules, /admin/users. New SYERP routes go inside the same
  <Route element={<AppShell />}> block.
From frontend/src/components/Sidebar.tsx: nav renders one NavLink per visible module at
  `/${mod.key}` (module key drives the path). SYERP module key determines the nav landing path.
From frontend/src/routes/syerp/Vendors.tsx + Customers.tsx (Plan 03): exported screen components.
shadcn primitives: Card, CardHeader, CardTitle, CardDescription, CardContent (installed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chart of Accounts browse screen + route wiring</name>
  <files>frontend/src/routes/syerp/GLAccounts.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/routes/admin/Users.tsx (analog: useQuery + loading/error state — read-only subset, no mutations)
    - frontend/src/App.tsx (the routing file being extended — current AppShell block)
    - frontend/src/components/Sidebar.tsx (how module key maps to nav path — confirm the SYERP landing route)
    - frontend/src/components/ui/card.tsx (Card primitives)
    - .planning/phases/04-syerp-core-hub/04-UI-SPEC.md (Screen 4 Chart of Accounts — 5 Card groups, row structure, copy, no toolbar/accent)
    - .planning/phases/04-syerp-core-hub/04-PATTERNS.md (GLAccounts.tsx section — grouped read-only tree, no mutation)
  </read_first>
  <action>
    Create `frontend/src/routes/syerp/GLAccounts.tsx`. Export `GLAccounts`. `useQuery` key
    `['syerp','gl','accounts']`, queryFn GETs `/api/v1/syerp/gl/accounts`. Render exactly per
    UI-SPEC Screen 4: page heading "Chart of Accounts" + description "General ledger account
    structure. Read-only in this version."; then five `<Card>` groups (Assets, Liabilities,
    Equity, Revenue, Expenses) — one per account_type — each with CardHeader (CardTitle +
    code-range CardDescription) and CardContent listing that type's accounts. Top-level (round-
    hundred) accounts get `font-semibold` and no indent; sub-accounts get `pl-6`. Row layout:
    mono code (`w-14`), name, type label (`ml-auto`). NO toolbar, NO mutations, NO accent
    elements (D-11 read-only). Loading = centered Loader2; error = the UI-SPEC error copy.
    Rows are plain non-interactive `div`s (accessibility rule 8).

    Then wire all SYERP routes into `frontend/src/App.tsx`: import Vendors, Customers (Plan 03)
    and GLAccounts, and add three `<Route>` entries INSIDE the existing
    `<Route element={<AppShell />}>` block: `/syerp/vendors` → Vendors, `/syerp/customers` →
    Customers, `/syerp/gl` → GLAccounts. Confirm against Sidebar.tsx that the SYERP module's
    nav key lands on one of these paths (the module key maps to `/${key}`); if the seeded SYERP
    module key is `syerp`, ensure a sensible index/redirect or that the nav entry points at a
    valid route (e.g. add a `/syerp` route or have the nav land on `/syerp/vendors`). Document
    the chosen mapping in the summary.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -q "syerp/vendors" src/App.tsx && grep -q "syerp/gl" src/App.tsx && grep -q "Chart of Accounts" src/routes/syerp/GLAccounts.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `GLAccounts.tsx` exports `GLAccounts`, fetches `/api/v1/syerp/gl/accounts`, and renders 5 account-type Cards with no toolbar, no mutation controls, and no accent elements.
    - `App.tsx` registers `/syerp/vendors`, `/syerp/customers`, and `/syerp/gl` inside the AppShell layout route, importing Vendors, Customers, and GLAccounts.
    - The SYERP nav entry resolves to a valid route (mapping documented).
    - `npx tsc --noEmit` is clean for the whole frontend.
  </acceptance_criteria>
  <done>CoA screen renders the seeded accounts read-only; all three SYERP screens are routed under the AppShell.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — Phase 4 SYERP screens end to end</name>
  <files>(verification only — no files modified)</files>
  <action>This is a human-verification checkpoint. The executor performs no edits. Present the what-built summary and the how-to-verify steps below to the user, pause, and wait for the resume signal before marking the phase complete.</action>
  <what-built>
    The full Phase 4 SYERP Core Hub: backend partner + GL API, and the Vendor, Customer, and
    Chart of Accounts screens wired into the app navigation. Vendor/Customer CRUD with
    server-side debounced search, Show-archived toggle, archive/restore, the shared partner edit
    form (Identity/Address/Contact/Commerce with role validation and settings-driven currency
    default), and a read-only seeded Chart of Accounts.
  </what-built>
  <how-to-verify>
    1. Ensure the stack is running with a fresh DB so the CoA seed and migration 0004 apply
       (`podman-compose up`, or run the backend + `npm run dev` for the SPA at :5173). Log in as
       the seeded admin (which has the syerp:read/syerp:write wildcard via the admin role).
    2. Confirm the sidebar shows the SYERP nav entry; open it and confirm you reach the Vendor
       (or SYERP landing) screen without a blank route.
    3. Vendors: click "Create Vendor". Verify the form pre-checks "Is Vendor", Currency defaults
       to the system default currency, and Save is disabled if you turn BOTH role switches off
       (inline "At least one role must be selected." appears). Create a vendor; confirm it
       appears in the list with an auto-generated P-#### code and an "Active" badge.
    4. Type in the search box; confirm the list narrows within ~300ms (server-side). Toggle
       "Show archived" on/off.
    5. Archive the vendor via the row menu → confirm dialog → it disappears from the default
       list; toggle Show archived and confirm it shows as "Archived"; use Restore (no
       confirmation) and confirm it returns to Active.
    6. Customers: repeat create + search on `/syerp/customers`. Create a partner with BOTH role
       switches on and confirm it appears in BOTH the Vendor and Customer lists.
    7. Chart of Accounts (`/syerp/gl`): confirm 5 grouped cards (Assets/Liabilities/Equity/
       Revenue/Expenses) render the seeded accounts read-only, with no add/edit controls.
    8. (Optional) Revoke syerp permissions from a non-admin user and confirm the nav entry hides
       and the API returns 403.
  </how-to-verify>
  <verify>Manual — user confirms the steps above pass and types the resume signal.</verify>
  <done>User has confirmed the end-to-end Phase 4 experience works (CRUD, search, archive/restore, dual-role, CoA browse).</done>
  <resume-signal>Type "approved" or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React UI → /api/v1/syerp/* | Backend require_permission is the real authz boundary; nav filtering is convenience |
| GL account data → DOM | Seeded reference strings rendered read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | GL browse mutability | mitigate | GLAccounts screen is read-only by construction — no Sheet/Dialog/mutation; D-11 scope guard enforced in UI |
| T-04-14 | Elevation of Privilege | SYERP nav visibility | mitigate | Nav entry shows only when module enabled ∩ user has syerp permission (Phase 3 D-04); verified in checkpoint step 8 |
| T-04-15 | Information Disclosure | GL endpoint exposure | mitigate | `GET /gl/accounts` gated by `require_permission("syerp:read")` (Plan 02 T-04-06) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- /syerp/vendors, /syerp/customers, /syerp/gl registered under AppShell.
- Human-verify checkpoint approved (full end-to-end CRUD, search, archive/restore, dual-role, CoA browse).
</verification>

<success_criteria>
- Chart of Accounts visible and browsable, read-only (SYERP-05).
- All three SYERP screens routed and reachable via nav.
- Human verification confirms the complete Phase 4 experience works.
</success_criteria>

<output>
After completion, create `.planning/phases/04-syerp-core-hub/04-04-SUMMARY.md`
</output>
