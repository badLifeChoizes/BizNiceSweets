---
phase: 04-syerp-core-hub
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - frontend/src/routes/syerp/Vendors.tsx
  - frontend/src/routes/syerp/Customers.tsx
  - frontend/src/routes/syerp/components/PartnerSheet.tsx
  - frontend/src/routes/syerp/components/PartnerArchiveDialog.tsx
  - frontend/src/routes/syerp/Vendors.test.tsx
  - frontend/src/routes/syerp/Customers.test.tsx
autonomous: true
requirements: [SYERP-01, SYERP-02, SYERP-03, SYERP-04]
must_haves:
  truths:
    - "The Vendor screen lists vendor partners, supports debounced search, and a Show archived toggle (D-02:, D-05:, D-07:)"
    - "The Customer screen lists customer partners with the same interactions"
    - "Create/Edit opens a shared PartnerSheet with Identity/Address/Contact/Commerce sections and at-least-one-role validation"
    - "Archive opens a confirmation dialog; Restore fires without confirmation (D-05:)"
    - "Mutations invalidate the role-scoped query so the list refreshes"
  artifacts:
    - path: "frontend/src/routes/syerp/Vendors.tsx"
      provides: "Vendor list screen (role=vendor)"
      contains: "Create Vendor"
    - path: "frontend/src/routes/syerp/Customers.tsx"
      provides: "Customer list screen (role=customer)"
      contains: "Create Customer"
    - path: "frontend/src/routes/syerp/components/PartnerSheet.tsx"
      provides: "Shared create/edit form parameterized by role"
      contains: "At least one role"
    - path: "frontend/src/routes/syerp/components/PartnerArchiveDialog.tsx"
      provides: "Archive confirmation dialog"
      contains: "Archive"
  key_links:
    - from: "frontend/src/routes/syerp/Vendors.tsx"
      to: "/api/v1/syerp/partners?role=vendor"
      via: "useQuery fetching server-side filtered/searched partners"
      pattern: "role=vendor"
    - from: "frontend/src/routes/syerp/components/PartnerSheet.tsx"
      to: "/api/v1/syerp/partners"
      via: "useMutation POST (create) / PATCH (edit) + invalidate role key"
      pattern: "invalidateQueries"
    - from: "frontend/src/routes/syerp/components/PartnerSheet.tsx"
      to: "/api/v1/core/settings"
      via: "currency default pre-populated from default_currency setting"
      pattern: "default_currency"
---

<objective>
Build the Vendor and Customer master-data screens and the shared partner edit
form + archive dialog, following Users.tsx exactly and the 04-UI-SPEC contract.
Server-side debounced search, Show-archived toggle, role-scoped query caches,
and the at-least-one-role validation in the form.

Purpose: These are the first real business-module screens. They surface the
unified partner table as the two conventional ERP screens (D-02) over a shared
form (RESEARCH Pattern 7).
Output: Vendors.tsx, Customers.tsx, PartnerSheet.tsx, PartnerArchiveDialog.tsx,
plus Wave 0 frontend tests. (Routing + nav wiring lands in Plan 04.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-syerp-core-hub/04-CONTEXT.md
@.planning/phases/04-syerp-core-hub/04-PATTERNS.md
@.planning/phases/04-syerp-core-hub/04-UI-SPEC.md
@.planning/phases/04-syerp-core-hub/04-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor consumes. -->

Backend API (Plan 02, served under /api/v1):
  GET  /api/v1/syerp/partners?role=vendor|customer&q=&include_archived=  -> PartnerRead[]
  POST /api/v1/syerp/partners                                            -> PartnerRead (201)
  GET  /api/v1/syerp/partners/{id}                                       -> PartnerRead
  PATCH /api/v1/syerp/partners/{id}  (body may set {active:false} to archive / {active:true} to restore)
  GET  /api/v1/core/settings  (existing) -> includes default_currency

PartnerRead fields: id, code, name, is_vendor, is_customer, active,
  addr_line1/line2/city/state/postal/country, contact_name/email/phone,
  payment_terms, tax_id, currency, country_of_origin, notes, created_at, updated_at.

From frontend/src/api/client.ts: apiClient (axios with 401 silent-refresh interceptor)
From frontend/src/routes/admin/Users.tsx: full interaction template (useQuery, useMutation,
  300ms debounceTimer, Sheet, Dialog, DropdownMenu, StatusBadge, loading/empty states).
  KEY DIFFERENCE: do NOT copy the client-side users.filter(...) — search is server-side via ?q=.
shadcn primitives available: table, dialog, sheet, input, label, select, button, card, badge,
  switch, separator, dropdown-menu (all installed).
Toast: sonner (installed Phase 3) — use for save/archive/restore feedback per UI-SPEC copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared PartnerSheet + PartnerArchiveDialog</name>
  <read_first>
    - frontend/src/routes/admin/Users.tsx (analog: Sheet section ~lines 387-500 for the form; Dialog section ~lines 502-545 for archive)
    - frontend/src/routes/admin/Settings.tsx (CURRENCY_OPTIONS list + how it reads settings)
    - frontend/src/api/client.ts (apiClient usage)
    - frontend/src/components/ui/ (Sheet, Dialog, Select, Switch, Separator, Input, Label, Button)
    - .planning/phases/04-syerp-core-hub/04-UI-SPEC.md ("Shared Partner Edit Form" section — exact fields, placeholders, copy, role-validation rule, currency default)
    - .planning/phases/04-syerp-core-hub/04-PATTERNS.md (PartnerSheet.tsx + PartnerArchiveDialog.tsx sections)
  </read_first>
  <action>
    Create `frontend/src/routes/syerp/components/PartnerSheet.tsx`. Props:
    `{ open, mode: 'create'|'edit', partner: PartnerRead|null, role: 'vendor'|'customer',
    onClose: () => void }`. Render a right-side `<Sheet side="right">` with four
    `<Separator>`-divided sections — Identity, Address, Contact, Commerce — exactly per UI-SPEC
    (every field, placeholder, and element type: Inputs, two role Switches, Payment Terms Select
    with options Net 30/Net 60/Net 90/Due on Receipt/Prepaid, Currency Select from Settings'
    CURRENCY_OPTIONS, Notes textarea styled like Input).

    Role validation: at least one of Is Vendor / Is Customer must be on. On create, pre-check the
    Switch matching `role`. If both off, show inline `<p className="text-sm text-destructive">At
    least one role must be selected.</p>` and disable Save.

    Currency default: on create-mount, read `default_currency` from `GET /api/v1/core/settings`
    (TanStack Query — cache hit from AppShell chain); fall back to "USD" if unavailable.

    Save: `useMutation` POST `/api/v1/syerp/partners` (create) or PATCH
    `/api/v1/syerp/partners/{id}` (edit). onSuccess invalidate `['syerp','partners',role]`, toast
    "Vendor saved."/"Customer saved." per role, call onClose. Sheet title/description/footer copy
    per the Copywriting Contract.

    Create `frontend/src/routes/syerp/components/PartnerArchiveDialog.tsx`. Props:
    `{ open, partner, role, onClose }`. `<Dialog>` with `aria-labelledby`/`aria-describedby`,
    destructive confirm. Confirm fires `useMutation` PATCH `/api/v1/syerp/partners/{id}` with
    `{active:false}`, invalidates the role key, toasts "Vendor archived."/"Customer archived.".
    Heading/body/buttons per UI-SPEC ("Archive vendor?", "Keep Vendor", "Archive Vendor"; the body
    notes references are preserved and restorable).

    Accessibility: every input has a paired Label (htmlFor/id); Sheet and Dialog have
    aria-labelledby + aria-describedby; archive confirm has `aria-label="Archive {name}"`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "syerp/components" || echo "no type errors in syerp/components"</automated>
  </verify>
  <acceptance_criteria>
    - `PartnerSheet.tsx` renders 4 Separator-divided sections with every field from the UI-SPEC and disables Save when both role switches are off, showing the inline "At least one role must be selected." error.
    - On create, the Currency Select defaults to the settings `default_currency` (or "USD").
    - Save uses POST for create / PATCH for edit and invalidates `['syerp','partners',role]`.
    - `PartnerArchiveDialog.tsx` confirm PATCHes `{active:false}` and invalidates the role key.
    - `npx tsc --noEmit` reports no type errors in the syerp/components files.
  </acceptance_criteria>
  <done>Shared form + archive dialog implement all D-03 fields, role validation, currency default, and audited mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Vendors and Customers list screens + Wave 0 frontend tests</name>
  <read_first>
    - frontend/src/routes/admin/Users.tsx (analog: full screen — toolbar, table, DropdownMenu actions, debounce, loading/empty states)
    - frontend/src/routes/admin/Users.test.tsx (analog for the Wave 0 tests — apiClient mock pattern)
    - frontend/src/routes/syerp/components/PartnerSheet.tsx + PartnerArchiveDialog.tsx (Task 1 — the components these screens mount)
    - .planning/phases/04-syerp-core-hub/04-UI-SPEC.md (Screen 1 Vendor List, Screen 2 Customer List — columns, copy, states)
    - .planning/phases/04-syerp-core-hub/04-PATTERNS.md (Vendors.tsx + Customers.tsx sections — differences from Users.tsx, including the server-side-search change)
  </read_first>
  <action>
    Create `frontend/src/routes/syerp/Vendors.tsx`. Export `Vendors`. Mirror Users.tsx but:
      - `useQuery` key `['syerp','partners','vendor',{ q, includeArchived }]`, queryFn GETs
        `/api/v1/syerp/partners?role=vendor&q={searchFilter}&include_archived={bool}` — server-side
        search; do NOT add a client-side `.filter()`.
      - 300ms `debounceTimer` drives `searchFilter` → re-fetch.
      - Toolbar: Search Input ("Search vendors…"), a `<Switch>` "Show archived", and the
        `variant="default"` "Create Vendor" button (the only accent element).
      - Table columns: Name | Code | Contact | Country | Status | Actions (per UI-SPEC).
        StatusBadge shows Active/Archived (color + text).
      - Row DropdownMenu: Edit → open PartnerSheet(edit); Archive → open PartnerArchiveDialog
        (when active); Restore → direct PATCH `{active:true}` + toast "Vendor restored." (when
        archived). aria-label `"Vendor actions for {name}"`.
      - Loading spinner + the two empty states (no records / no search match) with the exact copy.
      - Page heading "Vendors" + description per UI-SPEC.

    Create `frontend/src/routes/syerp/Customers.tsx` as the structural twin: role=customer, query
    key `['syerp','partners','customer',...]`, headings/placeholders/CTA "Customer"/"Create
    Customer", PartnerSheet `role="customer"`. Substitute every vendor reference with customer.

    Create the Wave 0 tests `Vendors.test.tsx` and `Customers.test.tsx` (mirror Users.test.tsx —
    mock apiClient): assert the screen renders its heading and its Create button, and that an
    empty API response renders the "No vendors yet"/"No customers yet" empty state. Keep them
    runnable under vitest.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/routes/syerp/Vendors.test.tsx src/routes/syerp/Customers.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Vendors.tsx` exports `Vendors`, fetches `?role=vendor`, performs server-side `?q=` search (no client-side filter), and renders columns Name/Code/Contact/Country/Status/Actions.
    - `Customers.tsx` exports `Customers`, fetches `?role=customer`, otherwise identical.
    - Both screens have a Show-archived Switch wired to `include_archived`, and Restore (no confirm) for archived rows.
    - `npm test -- --run src/routes/syerp/Vendors.test.tsx src/routes/syerp/Customers.test.tsx` passes (heading + Create button + empty state assertions green).
  </acceptance_criteria>
  <done>Vendor and Customer screens render, search server-side, toggle archived, and mount the shared form/dialog; Wave 0 frontend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React UI → /api/v1/syerp/* | Frontend nav/UI gating is convenience only; the backend require_permission (Plan 02) is the real authz boundary |
| partner name/text → DOM | Partner-supplied strings rendered into the table/form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering (XSS) | partner name/notes in table + sheet | mitigate | React renders text as text nodes (no dangerouslySetInnerHTML / innerHTML); no manual escaping needed |
| T-04-11 | Elevation of Privilege | client-side role gating | accept | UI hiding of actions is convenience; backend 403 (Plan 02) is authoritative — accepted per project authz model |
| T-04-12 | Information Disclosure | archived partners in default list | mitigate | Default fetch omits `include_archived` (server defaults active-only); Show-archived is an explicit user toggle |
</threat_model>

<verification>
- `npm test -- --run src/routes/syerp/Vendors.test.tsx src/routes/syerp/Customers.test.tsx` green.
- `npx tsc --noEmit` clean for the new syerp files.
- Search hits the server (?q=), not a client-side filter.
</verification>

<success_criteria>
- Vendor + Customer screens implement search, role filter, archive/restore, shared form (SYERP-01..04).
- PartnerSheet enforces at-least-one-role and defaults currency from settings.
- Wave 0 frontend tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-syerp-core-hub/04-03-SUMMARY.md`
</output>
