---
phase: 01-project-scaffolding-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/components.json
  - frontend/.eslintrc.cjs
  - frontend/.prettierrc.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/src/lib/queryClient.ts
  - frontend/src/routes/Landing.tsx
  - frontend/src/components/ui/.gitkeep
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "A Vite + React + TypeScript SPA exists and builds to static assets (npm run build produces frontend/dist)"
    - "Tailwind v4 is wired via @tailwindcss/vite (no tailwind.config.js) and shadcn/ui is initialized"
    - "React Router provides client-side routing with a landing/health page route"
    - "TanStack Query is wired (QueryClientProvider at app root)"
    - "The landing page fetches and displays backend health status via /api/v1 (or /health) through the query layer"
    - "D-07: Frontend is a Vite + React + TypeScript SPA using React Router for client-side routing and TanStack Query for data-fetching against the FastAPI backend"
  artifacts:
    - path: "frontend/package.json"
      provides: "Vite/React/TS/Tailwind v4/Router/TanStack pinned deps + build scripts"
      contains: "@tailwindcss/vite"
    - path: "frontend/vite.config.ts"
      provides: "Vite config with react + tailwindcss plugins, @ alias, base '/', polling-capable watch, /api proxy"
      contains: "usePolling"
    - path: "frontend/src/main.tsx"
      provides: "App bootstrap with QueryClientProvider and RouterProvider/BrowserRouter"
      contains: "QueryClientProvider"
    - path: "frontend/src/routes/Landing.tsx"
      provides: "Landing/health page that queries backend health"
      contains: "useQuery"
    - path: "frontend/src/index.css"
      provides: "Tailwind v4 entrypoint via @import"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "frontend/src/routes/Landing.tsx"
      to: "backend /health (proxied via /api in dev)"
      via: "TanStack useQuery fetch"
      pattern: "useQuery"
    - from: "frontend/src/main.tsx"
      to: "@tanstack/react-query"
      via: "QueryClientProvider"
      pattern: "QueryClientProvider"
    - from: "frontend/vite.config.ts"
      to: "@tailwindcss/vite"
      via: "vite plugin"
      pattern: "tailwindcss()"
---

<objective>
Stand up the frontend Vite + React 19 + TypeScript SPA skeleton (D-07): Tailwind v4 via `@tailwindcss/vite` (no `tailwind.config.js`), shadcn/ui initialized, React Router for client-side routing, TanStack Query for server state, and a minimal landing/health page that fetches backend health through the query layer. Windows-safe dev watching (polling-capable) and a `/api` proxy to the backend are configured.

Purpose: This is the static-asset frontend that Plan 03's backend container serves in production (D-08) and that the dev compose overlay runs with HMR (D-11). The skeleton establishes the routing + data-fetching conventions every later UI phase builds on.

Output: A frontend that `npm run build`s to `frontend/dist/` and `npm run dev`s with HMR, rendering a landing page that shows backend health status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffolding-deployment/01-CONTEXT.md
@.planning/phases/01-project-scaffolding-deployment/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TS, wire Tailwind v4 + shadcn, config + tooling</name>
  <files>frontend/package.json, frontend/index.html, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/components.json, frontend/.eslintrc.cjs, frontend/.prettierrc.json, frontend/src/index.css, frontend/src/lib/utils.ts, frontend/src/components/ui/.gitkeep</files>
  <read_first>
    - .planning/phases/01-project-scaffolding-deployment/01-RESEARCH.md (Core Frontend stack versions; Installation (frontend) steps; "Vite Config with Polling" example; Pitfall 5 Tailwind v4 vs v3 shadcn mismatch — @tailwindcss/vite, @import "tailwindcss", NO tailwind.config.js; State of the Art — CSS @import vs @tailwind directives)
    - .planning/phases/01-project-scaffolding-deployment/01-CONTEXT.md (D-07 Vite+React SPA, React Router, TanStack Query; Claude's Discretion: eslint/prettier scaffold light)
    - .planning/PROJECT.md (locked frontend stack: React 18+, TypeScript, Tailwind, shadcn/ui)
  </read_first>
  <action>
    Scaffold the Vite React-TS project under `frontend/` (`npm create vite@latest frontend -- --template react-ts`, then install). Pin the verified RESEARCH versions in `package.json`: vite 8.1.0, react 19.2.7, react-dom 19.2.7, typescript 6.0.3, @vitejs/plugin-react 6.0.3, tailwindcss 4.3.1, @tailwindcss/vite 4.3.1, react-router-dom 7.18.0, @tanstack/react-query 5.101.1, lucide-react 1.21.0; dev: eslint 10.5.0, prettier 3.8.4, typescript-eslint 8.62.0, @types/react 19.2.17. Scripts: `dev` (vite), `build` (tsc -b && vite build), `preview`, `lint`.

    Wire Tailwind v4 the v4 way (Pitfall 5): add `@tailwindcss/vite` to `vite.config.ts` plugins (alongside `react()`); make `frontend/src/index.css` the CSS entrypoint with `@import "tailwindcss";` (NOT `@tailwind base/components/utilities`). Do NOT create `tailwind.config.js`.

    Write `vite.config.ts` per the RESEARCH "Vite Config with Polling" example: plugins `[react(), tailwindcss()]`; resolve alias `@` → `./src`; `base: '/'`; `server.host: true`, `server.port: 5173`, `server.watch.usePolling: !!process.env.VITE_USE_POLLING` (Windows/WSL2 safe per Pitfall 3); `server.proxy['/api'] = 'http://api:8000'` so dev API calls reach the backend container.

    Initialize shadcn/ui (`npx shadcn@latest init`) producing `components.json` and `src/lib/utils.ts` (the `cn()` helper). Create an empty `frontend/src/components/ui/.gitkeep` so the shadcn component dir exists. Configure tsconfig path alias `@/*` → `src/*` across `tsconfig.json` / `tsconfig.app.json` so the `@` alias resolves.

    Add light tooling configs (Claude's discretion, keep light): `.eslintrc.cjs` (typescript-eslint recommended) and `.prettierrc.json`.
  </action>
  <verify>
    <automated>cd frontend && npm install --no-audit --no-fund >/dev/null 2>&1 && npx tsc -b --noEmit 2>&1 | tail -5; test -f vite.config.ts && test -f src/index.css && test ! -f tailwind.config.js && echo "v4-ok"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json depends on `@tailwindcss/vite`, `react-router-dom`, and `@tanstack/react-query` at the pinned RESEARCH versions
    - frontend/src/index.css contains `@import "tailwindcss"` and the repo does NOT contain `frontend/tailwind.config.js`
    - frontend/vite.config.ts contains `tailwindcss()`, `base: '/'`, `usePolling`, and a `/api` proxy
    - frontend/components.json and frontend/src/lib/utils.ts exist (shadcn initialized; utils exports `cn`)
    - `npx tsc -b --noEmit` reports no type errors
  </acceptance_criteria>
  <done>Frontend toolchain scaffolded with Tailwind v4 (no config file) + shadcn initialized, Windows-safe polling, `/api` proxy, and `@` alias resolving. Type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Router + TanStack Query providers + landing/health page</name>
  <files>frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/lib/queryClient.ts, frontend/src/routes/Landing.tsx</files>
  <read_first>
    - .planning/phases/01-project-scaffolding-deployment/01-RESEARCH.md (D-07 React Router + TanStack Query; System Architecture Diagram — /api proxy to backend; Health Check Endpoint — /health/live, /health/ready shapes)
    - .planning/phases/01-project-scaffolding-deployment/01-CONTEXT.md (phase boundary: minimal landing/health page only — NO app shell, settings, or module-toggle UI; those are Phase 3)
    - frontend/src/lib/utils.ts, frontend/vite.config.ts (created in Task 1 — proxy + cn helper)
  </read_first>
  <action>
    Create `frontend/src/lib/queryClient.ts` exporting a configured `QueryClient` instance (sensible defaults; e.g. no aggressive refetch). 

    Create `frontend/src/main.tsx` bootstrapping the SPA: wrap `<App/>` in `QueryClientProvider` (using the exported client) and a React Router `BrowserRouter`. Import `./index.css`.

    Create `frontend/src/App.tsx` defining routes via React Router (`Routes`/`Route`): a single index route rendering `Landing`. Keep it minimal — NO navigation shell, NO module list, NO settings (Phase 3 scope).

    Create `frontend/src/routes/Landing.tsx`: a Tailwind-styled landing/health page that uses TanStack `useQuery` to GET `/api/health/live` (and optionally `/api/health/ready`) via `fetch` (dev proxy forwards `/api` → backend; in production both are same-origin under the backend container). Display the returned status (loading / connected / error states). This proves the frontend↔backend wiring and the query layer convention. Use the shadcn `cn` helper / Tailwind classes; do not pull in heavy components.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit 2>&1 | tail -5 && npm run build >/dev/null 2>&1 && test -d dist && echo "build-ok"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/main.tsx contains `QueryClientProvider` and a Router provider (`BrowserRouter` or `RouterProvider`)
    - frontend/src/routes/Landing.tsx contains `useQuery` and references a `/api/health` endpoint
    - frontend/src/App.tsx wires a route to `Landing` and contains NO settings/module-toggle/app-shell components
    - `npm run build` succeeds and produces `frontend/dist/`
    - `npx tsc -b --noEmit` reports no type errors
  </acceptance_criteria>
  <done>SPA renders a landing/health page that fetches backend health through TanStack Query under React Router; builds to static `frontend/dist/` for the backend container to serve (Plan 03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SPA static assets | the built bundle is public; must contain no secrets |
| SPA → backend /api | client fetches health; same-origin in prod, proxied in dev |
| npm registry → build | third-party JS supply chain enters at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | frontend bundle / env | mitigate | No secrets in frontend env; only `VITE_*` non-secret config (e.g. `VITE_API_BASE_URL`) is exposed to the client. DB credentials never reach the frontend |
| T-01-08 | Tampering | dependency supply chain | mitigate | Pin exact RESEARCH-verified versions in `package.json`; `npm ci` against a committed lockfile in the container build (Plan 03 Dockerfile uses `npm ci`) |
| T-01-09 | Spoofing | dev `/api` proxy target | accept | Proxy targets internal compose service `api:8000`; dev-only, not present in production static build. Low risk on a self-hosted single-operator box |
</threat_model>

<verification>
- `npx tsc -b --noEmit` passes (no type errors).
- `npm run build` produces `frontend/dist/`.
- No `frontend/tailwind.config.js` (v4 wiring); `src/index.css` uses `@import "tailwindcss"`.
- Landing page uses `useQuery` against `/api/health`.
- Full end-to-end (frontend served by backend, live health) is verified in Plan 03 compose run.
</verification>

<success_criteria>
- Vite + React + TS SPA builds to static assets (D-07, D-08 prerequisite).
- Tailwind v4 + shadcn initialized correctly (Pitfall 5 avoided).
- React Router + TanStack Query wired; landing page fetches backend health.
- Windows-safe polling + `/api` proxy configured for the dev overlay (D-11).
- No HIGH-severity threat unmitigated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-deployment/01-02-SUMMARY.md`.
</output>
