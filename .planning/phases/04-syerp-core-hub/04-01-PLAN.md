---
phase: 04-syerp-core-hub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/modules/syerp/models.py
  - backend/app/modules/syerp/coa_seed.py
  - backend/app/core/seed.py
  - backend/alembic/versions/0004_syerp_tables.py
  - backend/tests/syerp/__init__.py
  - backend/tests/syerp/conftest.py
  - backend/tests/syerp/test_partners.py
  - backend/tests/syerp/test_gl.py
autonomous: true
requirements: [SYERP-01, SYERP-05]
must_haves:
  truths:
    - "syerp_partner and syerp_gl_account tables exist in the schema after migration"
    - "GL accounts are seeded with a standard chart-of-accounts on startup"
    - "Re-running the CoA seed does not duplicate accounts (idempotent)"
    - "Backend test scaffold for partner CRUD, search, archive, and GL exists and is collectable by pytest"
  artifacts:
    - path: "backend/app/modules/syerp/models.py"
      provides: "Partner + GLAccount SQLAlchemy 2.0 models"
      contains: "class Partner"
    - path: "backend/app/modules/syerp/coa_seed.py"
      provides: "Idempotent CoA seed function seed_gl_accounts"
      contains: "_STANDARD_COA"
    - path: "backend/alembic/versions/0004_syerp_tables.py"
      provides: "Migration creating both syerp tables"
      contains: "syerp_partner"
    - path: "backend/tests/syerp/test_partners.py"
      provides: "Wave 0 partner test stubs"
      contains: "def test_create_vendor"
    - path: "backend/tests/syerp/test_gl.py"
      provides: "Wave 0 GL test stubs"
      contains: "def test_gl_accounts_seeded"
  key_links:
    - from: "backend/app/core/models.py"
      to: "backend/app/modules/syerp/models.py"
      via: "existing import of syerp_models (line 15) picks up Partner + GLAccount"
      pattern: "from app.modules.syerp import models"
    - from: "backend/app/core/seed.py"
      to: "backend/app/modules/syerp/coa_seed.py"
      via: "run_seeds() calls seed_gl_accounts(db)"
      pattern: "seed_gl_accounts"
---

<objective>
Lay the SYERP backend data foundation: the two tables every downstream module
FKs into (`syerp_partner`, `syerp_gl_account`), the idempotent chart-of-accounts
seed, the Alembic migration that creates both tables, and the Wave 0 test
scaffold that the next plan greens.

Purpose: All Phase 4 API and UI work depends on these tables and the seeded CoA.
The Wave 0 tests must exist first so Nyquist sampling has automated verifications
to run from the very next task.
Output: Partner + GLAccount models, CoA seed wired into `run_seeds()`, autogenerated
migration `0004`, and `backend/tests/syerp/` with failing/skipping test stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-syerp-core-hub/04-CONTEXT.md
@.planning/phases/04-syerp-core-hub/04-RESEARCH.md
@.planning/phases/04-syerp-core-hub/04-PATTERNS.md

<interfaces>
<!-- Concrete contracts the executor must replicate. Extracted from codebase. -->

From backend/app/modules/auth/models.py — SQLAlchemy 2.0 Mapped[] style (MANDATORY):
  id: Mapped[str] = mapped_column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
  name: Mapped[str] = mapped_column(String(255), nullable=False, index=True)
  active: Mapped[bool] = mapped_column(Boolean, default=True, nullable=False)
  created_at / updated_at use DateTime(timezone=True) with default=lambda: datetime.now(timezone.utc)
  Integer PK form (GLAccount): id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)

From backend/app/core/seed.py — run_seeds() body imports each seed fn locally then awaits:
  await seed_admin_user(db); await seed_modules_table(db); await seed_default_settings(db)

From backend/app/modules/auth/seed.py — idempotent select-before-insert:
  result = await db.execute(select(Model).where(Model.code == code))
  if result.scalars().first() is None: db.add(Model(...)); await db.flush()
  single await db.commit() at end

From backend/tests/conftest.py — fixtures available: client (httpx.AsyncClient), skip_if_no_db
From backend/tests/auth/conftest_helpers.py — admin_login_token(client), create_regular_user
From backend/app/modules/auth/service.py — create_access_token(subject: str, permissions: list[str]) -> str
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Partner + GLAccount models and the SYERP migration</name>
  <read_first>
    - backend/app/modules/syerp/models.py (the stub being filled — current state)
    - backend/app/modules/auth/models.py (analog: Mapped[] style, uuid PK, timestamps, lazy="selectin")
    - backend/app/core/models.py (confirm line 15 already imports syerp models — no change needed here)
    - backend/alembic/versions/ (read the most recent existing migration to match autogenerate output shape and down_revision chaining)
    - .planning/phases/04-syerp-core-hub/04-RESEARCH.md (Pattern 1, Pattern 2 — exact column sets)
    - .planning/phases/04-syerp-core-hub/04-PATTERNS.md (models.py section — CRITICAL notes on `active` vs `is_active` and `account_type` vs `type`)
  </read_first>
  <action>
    In `backend/app/modules/syerp/models.py`, replace the stub body with two SQLAlchemy 2.0
    models using the `Mapped[type] = mapped_column(...)` style.

    `Partner` → `__tablename__ = "syerp_partner"`:
      - `id` String(36) UUID PK (default lambda str(uuid4)).
      - Identity: `code` String(20) unique=True nullable=False index=True; `name` String(255)
        nullable=False index=True; `is_vendor` Boolean default=False nullable=False;
        `is_customer` Boolean default=False nullable=False; `active` Boolean default=True
        nullable=False index=True (use `active`, NOT `is_active`).
      - Address (all nullable): addr_line1/addr_line2 String(255), addr_city String(100),
        addr_state String(100), addr_postal String(20), addr_country String(2).
      - Contact (all nullable): contact_name String(255), contact_email String(255),
        contact_phone String(50).
      - Commerce (all nullable): payment_terms String(50), tax_id String(50),
        currency String(3), country_of_origin String(2), notes Text.
      - Timestamps: created_at, updated_at (timezone-aware, with onupdate on updated_at).

    `GLAccount` → `__tablename__ = "syerp_gl_account"`:
      - `id` Integer PK autoincrement=True.
      - `code` String(10) unique=True nullable=False index=True.
      - `name` String(255) nullable=False.
      - `account_type` String(20) nullable=False (use `account_type`, NEVER `type` — shadows
        Python builtin / SQLAlchemy reserved).
      - `parent_id` Integer nullable=True ForeignKey("syerp_gl_account.id") (self-referential).
      - `active` Boolean default=True nullable=False.
      Neither model declares ORM relationships in this phase (the GL endpoint returns a flat
      list; tree is built frontend-side) — avoids MissingGreenlet (Pitfall 2).

    Do NOT modify `core/models.py` — its existing `from app.modules.syerp import models`
    import (line 15) auto-registers both classes for Alembic.

    Then generate the migration: run
    `alembic revision --autogenerate -m "add syerp_partner and syerp_gl_account tables"`
    from `backend/`. Rename the produced file so its revision id / filename reads
    `0004_syerp_tables.py` and its `down_revision` chains onto the latest existing head.
    Confirm the migration creates both tables with the unique constraint on `code` and the
    self-referential FK on `syerp_gl_account.parent_id`.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.modules.syerp.models import Partner, GLAccount; print(Partner.__tablename__, GLAccount.__tablename__)" && grep -q "syerp_partner" alembic/versions/0004_syerp_tables.py && grep -q "syerp_gl_account" alembic/versions/0004_syerp_tables.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/modules/syerp/models.py` defines `class Partner` with `__tablename__ = "syerp_partner"` and `class GLAccount` with `__tablename__ = "syerp_gl_account"`.
    - Partner uses `active: Mapped[bool]` (NOT `is_active`); GLAccount uses `account_type` (NOT `type`).
    - Importing `from app.modules.syerp.models import Partner, GLAccount` succeeds with no error.
    - `backend/alembic/versions/0004_syerp_tables.py` exists, contains `syerp_partner` and `syerp_gl_account`, declares a unique constraint on partner `code`, and a FK on `syerp_gl_account.parent_id`; its `down_revision` points at the prior migration head (no broken chain).
    - No edits were made to `backend/app/core/models.py`.
  </acceptance_criteria>
  <done>Both tables defined in the SYERP stub; migration 0004 creates them and chains onto the existing Alembic head.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent CoA seed wired into run_seeds()</name>
  <read_first>
    - backend/app/core/seed.py (the run_seeds hook being extended — current state)
    - backend/app/modules/auth/seed.py (analog: select-before-insert idempotency, flush+single commit, TYPE_CHECKING import guard)
    - backend/app/modules/syerp/models.py (the GLAccount model created in Task 1)
    - .planning/phases/04-syerp-core-hub/04-RESEARCH.md (Pattern 8 + "Standard Chart of Accounts Seed" — the 40-account list and two-pass parent_id resolution)
    - .planning/phases/04-syerp-core-hub/04-PATTERNS.md (coa_seed.py + core/seed.py sections)
  </read_first>
  <action>
    Create `backend/app/modules/syerp/coa_seed.py` with a module-level `_STANDARD_COA` list
    (the ~40 accounts from RESEARCH.md "Standard Chart of Accounts Seed": 5 GAAP types,
    1xxx Assets / 2xxx Liabilities / 3xxx Equity / 4xxx Revenue / 5xxx Expenses, including the
    manufacturing accounts WIP, COGS sub-accounts, R&D). Each entry carries `code`, `name`,
    `account_type`, and the parent account's code (or None for top-level).

    Implement `async def seed_gl_accounts(db) -> None` following the idempotent
    select-before-insert pattern: for each account, SELECT GLAccount by `code`; if absent,
    insert. Resolve `parent_id` by code: insert parents (parent code None) first and flush to
    obtain their integer ids, build a `{code: id}` map, then insert children with the resolved
    `parent_id` (Pitfall 6 — parents before children). Single `await db.commit()` at the end.
    Use the runtime-local import of GLAccount inside the function body (mirror auth/seed.py)
    and the TYPE_CHECKING guard for AsyncSession.

    Then wire it into `backend/app/core/seed.py`: inside `run_seeds()`, after
    `await seed_default_settings(db)`, add the local import
    `from app.modules.syerp.coa_seed import seed_gl_accounts` and `await seed_gl_accounts(db)`.
    Update the run_seeds docstring to note Phase 4 seeds the CoA.
  </action>
  <verify>
    <automated>cd backend && grep -q "seed_gl_accounts" app/core/seed.py && python -c "from app.modules.syerp.coa_seed import seed_gl_accounts, _STANDARD_COA; assert len(_STANDARD_COA) >= 40, len(_STANDARD_COA); types={a['account_type'] for a in _STANDARD_COA}; assert types == {'ASSET','LIABILITY','EQUITY','REVENUE','EXPENSE'}, types; print('coa ok', len(_STANDARD_COA))"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/modules/syerp/coa_seed.py` exports `seed_gl_accounts` and `_STANDARD_COA`.
    - `_STANDARD_COA` has >= 40 entries spanning exactly the 5 types `ASSET, LIABILITY, EQUITY, REVENUE, EXPENSE`.
    - The seed function uses select-before-insert (no duplicate inserts on re-run) and resolves `parent_id` via a code→id map with parents inserted before children.
    - `backend/app/core/seed.py` `run_seeds()` calls `await seed_gl_accounts(db)` after `seed_default_settings`, and `grep seed_gl_accounts app/core/seed.py` matches.
  </acceptance_criteria>
  <done>CoA seed is idempotent, contains the standard manufacturing-oriented account set, and runs on every startup via run_seeds().</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 — SYERP backend test scaffold</name>
  <read_first>
    - backend/tests/conftest.py (available fixtures: client, skip_if_no_db, DB skip behavior)
    - backend/tests/auth/test_user_admin.py (analog: admin_login_token usage, create_access_token token minting for 403/401, audit-log assertion pattern)
    - backend/tests/auth/conftest_helpers.py (admin_login_token, create_regular_user helpers)
    - .planning/phases/04-syerp-core-hub/04-RESEARCH.md ("Phase Requirements → Test Map" — exact test names per requirement)
    - .planning/phases/04-syerp-core-hub/04-PATTERNS.md (test_partners.py + test_gl.py sections)
  </read_first>
  <action>
    Create `backend/tests/syerp/__init__.py` (empty package marker) and a
    `backend/tests/syerp/conftest.py` if any shared partner-factory helper is needed (otherwise
    reuse `tests/auth/conftest_helpers`). Create the two test modules below as Wave 0 stubs.
    Each test uses the `client` and `skip_if_no_db` fixtures, mints tokens via
    `create_access_token(subject=..., permissions=[...])` or `admin_login_token(client)`, and
    targets routes under `/api/v1/syerp/...` (the full prefix as the running app exposes them).

    `backend/tests/syerp/test_partners.py` — one test function per row in the RESEARCH Test Map
    for SYERP-01..04, named exactly: test_create_vendor, test_create_requires_role,
    test_update_partner_writes_audit, test_archive_partner, test_archived_excluded_by_default,
    test_create_requires_syerp_write, test_duplicate_code_rejected, test_search_by_name,
    test_search_by_code, test_vendor_role_filter, test_create_customer, test_customer_role_filter,
    test_dual_role_appears_in_both.

    `backend/tests/syerp/test_gl.py` — test_gl_accounts_seeded, test_gl_seed_idempotent,
    test_gl_requires_syerp_read.

    Write each as a REAL test asserting the intended behavior (status codes, body fields, audit
    rows, role filtering) — these will fail/skip now because the API does not exist yet, and
    Plan 02 greens them. They MUST be collectable (import cleanly, valid async test signatures)
    so `pytest --collect-only` succeeds. Do not mark them xfail; rely on `skip_if_no_db` for the
    no-DB case and real assertions for the behavior.
  </action>
  <verify>
    <automated>cd backend && pytest tests/syerp/ --collect-only -q 2>&1 | grep -E "test_create_vendor|test_gl_accounts_seeded" && pytest tests/syerp/ --collect-only -q 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/syerp/__init__.py` exists.
    - `pytest tests/syerp/ --collect-only -q` succeeds (no import/collection errors) and lists `test_create_vendor`, `test_create_requires_role`, `test_archive_partner`, `test_search_by_name`, `test_vendor_role_filter`, `test_customer_role_filter`, `test_dual_role_appears_in_both`, `test_gl_accounts_seeded`, `test_gl_seed_idempotent`, `test_gl_requires_syerp_read`.
    - Each test references `/api/v1/syerp/` routes and uses `client` + `skip_if_no_db` fixtures.
    - No test is marked xfail; behavior is asserted with real status-code / field / audit assertions.
  </acceptance_criteria>
  <done>SYERP test package collects cleanly with real behavior assertions for SYERP-01..05, ready for Plan 02 to green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → DB schema | Migration defines the unique/FK constraints that act as integrity guards for later API code |
| Seed → DB | Startup seed writes the CoA; must be idempotent so repeated `podman-compose up` cannot corrupt/duplicate reference data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `syerp_partner.code` uniqueness | mitigate | Unique DB constraint declared in migration 0004 is the true guard against duplicate codes (race-safe at DB level), independent of service-layer generation |
| T-04-02 | Integrity | CoA seed re-run | mitigate | select-before-insert idempotency: re-running `run_seeds` cannot duplicate accounts (verified by `test_gl_seed_idempotent`) |
| T-04-03 | Denial of Service | self-referential FK on `parent_id` | accept | Two-pass parent-before-child insert ordering prevents FK violation at seed time; 40 static rows, no runtime DoS surface |
</threat_model>

<verification>
- `python -c "from app.modules.syerp.models import Partner, GLAccount"` imports cleanly.
- Migration 0004 file contains both table names and chains onto the prior head.
- `pytest tests/syerp/ --collect-only -q` collects all named tests with no errors.
- `_STANDARD_COA` has >= 40 accounts across the 5 GAAP types.
</verification>

<success_criteria>
- syerp_partner + syerp_gl_account models defined in the stub (no new model files).
- Migration 0004 creates both tables with the code-unique constraint and parent_id FK.
- CoA seed wired into run_seeds(), idempotent, manufacturing-oriented account set.
- Wave 0 SYERP test scaffold collects cleanly with real assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-syerp-core-hub/04-01-SUMMARY.md`
</output>
