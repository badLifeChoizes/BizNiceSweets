---
phase: 05-plum-parts-revisions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/modules/plum/__init__.py
  - backend/app/modules/plum/models.py
  - backend/app/modules/plum/schemas.py
  - backend/app/modules/plum/seed.py
  - backend/app/core/models.py
  - backend/app/core/seed.py
  - backend/alembic/versions/0005_plum_tables.py
  - backend/tests/plum/__init__.py
  - backend/tests/plum/test_parts.py
  - backend/tests/plum/test_revisions.py
autonomous: true
requirements: [PLUM-01, PLUM-02, PLUM-03]
must_haves:
  truths:
    - "The plum_part, plum_part_revision, plum_classification_tag, and plum_part_tag tables exist after migration 0005 applies"
    - "Alembic autogenerate discovers the PLUM models (core/models.py imports them)"
    - "The classification-tag starter vocabulary and PLUM default settings seed idempotently on startup"
    - "A partial unique index enforces at most one Released revision per part at the DB level"
  artifacts:
    - path: "backend/app/modules/plum/models.py"
      provides: "PlumPart, PlumPartRevision, PlumClassificationTag, PlumPartTag SQLAlchemy models"
      contains: "class PlumPart"
    - path: "backend/app/modules/plum/schemas.py"
      provides: "PartCreate, PartUpdate, PartRead, RevisionCreate, RevisionRead, PartDetailRead Pydantic schemas"
      contains: "class PartCreate"
    - path: "backend/app/modules/plum/seed.py"
      provides: "seed_plum_data() — tag vocabulary + plum settings"
      contains: "async def seed_plum_data"
    - path: "backend/alembic/versions/0005_plum_tables.py"
      provides: "Migration creating the four plum_ tables + partial unique index"
      contains: "down_revision"
    - path: "backend/tests/plum/test_parts.py"
      provides: "Wave 0 PLUM-01/PLUM-02 backend test scaffold"
      contains: "def test_create_part"
    - path: "backend/tests/plum/test_revisions.py"
      provides: "Wave 0 PLUM-03 revision FSM test scaffold"
      contains: "def test_release_supersedes_prior"
  key_links:
    - from: "backend/app/core/models.py"
      to: "app.modules.plum.models"
      via: "uncommented import for Alembic autogenerate discovery"
      pattern: "from app.modules.plum import models"
    - from: "backend/app/core/seed.py"
      to: "seed_plum_data"
      via: "run_seeds() call"
      pattern: "seed_plum_data"
---

<objective>
Stand up the PLUM module's backend data layer: the two-table revision model (D-01/D-02), the classification-tag tables (D-12), the Pydantic schemas, the idempotent seed, Alembic migration 0005, and the Wave 0 test scaffold the rest of the phase will green.

Purpose: Every downstream plan (service/router in 05-02, frontend in 05-03/05-04) depends on these tables, schemas, and the test harness existing. This plan defines the contracts first so executors build against them, not toward them.

Output: The `backend/app/modules/plum/` package (models, schemas, seed, __init__ stub), the uncommented core aggregator import, the extended run_seeds(), migration 0005, and `backend/tests/plum/` failing-but-importable test scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plum-parts-revisions/05-CONTEXT.md
@.planning/phases/05-plum-parts-revisions/05-RESEARCH.md
@.planning/phases/05-plum-parts-revisions/05-PATTERNS.md

<interfaces>
<!-- Settings table the PLUM seed writes into — already exists (Phase 3). -->
From backend/app/core/settings_model.py:
- class Setting(Base): __tablename__ = "settings"; columns: id (int PK), key (String(100), index), value (Text nullable), value_type (String(20) default "str"), owner_id (nullable). Partial unique index on key WHERE owner_id IS NULL.

<!-- Seed extension point — already exists, append to it. -->
From backend/app/core/seed.py run_seeds():
- Imports + awaits: seed_admin_user, seed_modules_table, seed_default_settings, seed_gl_accounts (in that order). Append `from app.modules.plum.seed import seed_plum_data` + `await seed_plum_data(db)` after seed_gl_accounts.

<!-- Aggregator stub — already present at line 27, commented. -->
From backend/app/core/models.py:
- Line 27: `# from app.modules.plum import models as plum_models    # noqa: F401` — uncomment.

<!-- Permissions already seeded (Phase 2) — do NOT re-seed. -->
plum:read / plum:write already in backend/app/modules/auth/seed.py lines 36-38; PLUM module already in modules_seed.py line 25 (always_on=False, sort_order 20).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define PLUM models, schemas, and module __init__ stub</name>
  <files>backend/app/modules/plum/__init__.py, backend/app/modules/plum/models.py, backend/app/modules/plum/schemas.py</files>
  <read_first>
    - backend/app/modules/syerp/models.py (the model analog — copy the Partner class shape, the plum_ table-name prefix convention, the "no ORM relationships in async" comment, the GLAccount integer-PK pattern for the tag lookup table)
    - backend/app/modules/syerp/schemas.py (the schema analog — PartnerCreate optional-code pattern, PartnerUpdate all-Optional + exclude_unset PATCH semantics, PartnerRead from_attributes pattern, model_validator pattern)
    - backend/app/modules/syerp/__init__.py (the module-init analog — copy verbatim, replace syerp→plum)
    - backend/app/core/base.py (the Base declarative class all models inherit)
    - .planning/phases/05-plum-parts-revisions/05-PATTERNS.md (Pattern Assignments for models.py and schemas.py — exact column sets and field shapes)
    - .planning/phases/05-plum-parts-revisions/05-RESEARCH.md (Pattern 2 two-table model, Pattern 5 tags join table, Detailed Design Recommendations for column/type/index choices)
  </read_first>
  <action>
    Create the `backend/app/modules/plum/` package with three files.

    In `__init__.py`: mirror `syerp/__init__.py` exactly — set `MODULE_NAME = "plum"`, import `from app.modules.plum.router import router  # noqa: F401`, and call `registry.register(sys.modules[__name__])`. NOTE: the router import will fail until plan 05-02 creates router.py; create a minimal placeholder `router.py` with `from fastapi import APIRouter; router = APIRouter(prefix="/plum", tags=["plum"])` so this package imports cleanly now and 05-02 fleshes it out. The model aggregator import (Task 2) does not depend on the router.

    In `models.py`: define four SQLAlchemy 2.0 models inheriting `Base`, all with `plum_` table-name prefix, NO ORM relationships (use explicit select queries in the service per the async MissingGreenlet pitfall — copy the warning comment from syerp/models.py and adapt it):
      - `PlumPart` (`__tablename__ = "plum_part"`): id (String(36) PK, uuid4 default), part_number (String(50), unique, nullable=False, index=True), active (Boolean, default True, nullable=False, index=True), created_at / updated_at (DateTime timezone-aware, onupdate on updated_at).
      - `PlumClassificationTag` (`__tablename__ = "plum_classification_tag"`): id (Integer PK autoincrement), name (String(100), unique, nullable=False), sort_order (Integer), active (Boolean default True).
      - `PlumPartTag` (`__tablename__ = "plum_part_tag"`): part_id (String(36) FK→plum_part.id, PK), tag_id (Integer FK→plum_classification_tag.id, PK) — composite primary key join table.
      - `PlumPartRevision` (`__tablename__ = "plum_part_revision"`): id (String(36) PK uuid4), part_id (String(36) FK→plum_part.id, nullable=False, index=True), revision_number (Integer, nullable=False — per-part sequence 1,2,3 used for "latest revision" resolution per RESEARCH Pattern 4/Open Question 2), revision_label (String(20), nullable=False — "A" or "0.1.0"), status (String(20), nullable=False, default "draft", index=True — one of draft/in_review/released/obsolete), and the D-02 snapshot columns: description (String(500), nullable=False), category (String(100), nullable=True), unit_of_measure (String(50), nullable=True), notes (Text, nullable=True), reason_for_revision (Text, nullable=True), plus created_at / released_at (nullable) / obsoleted_at (nullable) DateTime timezone-aware. Add `__table_args__` with composite index `ix_plum_part_revision_part_id_status` on (part_id, status).
  </action>
  <verify>
    <automated>cd backend && python -c "from app.modules.plum import models; from app.modules.plum import schemas; print(models.PlumPart.__tablename__, models.PlumPartRevision.__tablename__, models.PlumClassificationTag.__tablename__, models.PlumPartTag.__tablename__)"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/modules/plum/models.py` contains `class PlumPart`, `class PlumPartRevision`, `class PlumClassificationTag`, `class PlumPartTag`
    - `PlumPart.__tablename__ == "plum_part"` and all four table names carry the `plum_` prefix
    - `PlumPartRevision` has columns: part_id, revision_number, revision_label, status, description, category, unit_of_measure, notes, reason_for_revision, created_at, released_at, obsoleted_at
    - No `relationship(` call appears in models.py (grep returns 0): `grep -c "relationship(" backend/app/modules/plum/models.py` → 0
    - `backend/app/modules/plum/schemas.py` contains `class PartCreate`, `class PartUpdate`, `class PartRead`, `class RevisionCreate`, `class RevisionRead`, `class PartDetailRead`
    - `PartCreate` requires only `description` (str) and makes `part_number` Optional (server auto-gens) per D-12
    - The verify command imports both modules and prints the four table names without error
  </acceptance_criteria>
  <done>The plum package imports cleanly; all four models and all six schemas are defined per D-01/D-02/D-07/D-12 with no ORM relationships.</done>
</task>

<task type="auto">
  <name>Task 2: Wire model discovery, seed, and generate migration 0005</name>
  <files>backend/app/core/models.py, backend/app/modules/plum/seed.py, backend/app/core/seed.py, backend/alembic/versions/0005_plum_tables.py</files>
  <read_first>
    - backend/app/core/models.py (uncomment the existing line-27 stub)
    - backend/app/core/seed.py (run_seeds() — append the plum seed call after seed_gl_accounts following the existing import+await convention)
    - backend/app/modules/syerp/coa_seed.py (the seed analog — idempotent select-before-insert pattern, async def signature, db.commit at end)
    - backend/app/core/settings_seed.py (Setting select-before-insert pattern for the two plum.* setting rows)
    - backend/alembic/versions/0004_syerp_tables.py (the migration analog — hand-authored convention, header revision/down_revision, op.create_table ordering, op.create_index, downgrade reverse order)
    - .planning/phases/05-plum-parts-revisions/05-PATTERNS.md (seed.py and 0005 migration Pattern Assignments — table creation order tags→part→part_tag→revision, partial unique index syntax)
  </read_first>
  <action>
    Uncomment line 27 of `backend/app/core/models.py` to `from app.modules.plum import models as plum_models  # noqa: F401` so Alembic's Base.metadata sees the PLUM tables.

    Create `backend/app/modules/plum/seed.py` with `async def seed_plum_data(db)` mirroring `coa_seed.py`'s idempotent select-before-insert pattern. Seed: (1) the six classification tags — Purchased(1), Manufactured(2), Assembly(3), Finished Good(4), Tool(5), Raw Material(6) — checking `PlumClassificationTag.name` existence before insert; (2) two `Setting` rows — `plum.revision_scheme`="asme" and `plum.tag_vocabulary_editable`="true" — checking `Setting.key` existence before insert (D-04, D-12). End with `await db.commit()`.

    Extend `backend/app/core/seed.py` run_seeds(): after the `await seed_gl_accounts(db)` line, add `from app.modules.plum.seed import seed_plum_data` and `await seed_plum_data(db)`.

    Hand-author `backend/alembic/versions/0005_plum_tables.py` following the 0004 convention: `revision = "0005"`, `down_revision = "0004"`. In upgrade(), create tables in dependency order: plum_classification_tag → plum_part → plum_part_tag → plum_part_revision, with all columns matching the models, FKs, and the indexes (part_number unique index, active index, revision part_id index, status index, composite part_id+status index). Then create the partial unique index `uq_plum_part_one_released` ON plum_part_revision(part_id) `postgresql_where=sa.text("status = 'released'")` (RESEARCH Pitfall 3 / Open Question 1 — DB-level one-Released-per-part invariant). downgrade() drops the index then the four tables in reverse order.
  </action>
  <verify>
    <automated>cd backend && python -c "import app.core.models" && python -c "import ast,sys; t=ast.parse(open('backend/alembic/versions/0005_plum_tables.py').read()) if False else None" ; grep -q 'down_revision.*=.*\"0004\"' backend/alembic/versions/0005_plum_tables.py && grep -q "uq_plum_part_one_released" backend/alembic/versions/0005_plum_tables.py && grep -q "seed_plum_data" backend/app/core/seed.py && python -c "import ast; ast.parse(open('backend/alembic/versions/0005_plum_tables.py').read()); ast.parse(open('backend/app/modules/plum/seed.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/core/models.py` line for plum is uncommented: `grep -v '^#' backend/app/core/models.py | grep -c "from app.modules.plum import models"` → 1
    - `cd backend && python -c "import app.core.models"` exits 0 (PLUM models importable via aggregator)
    - `backend/alembic/versions/0005_plum_tables.py` contains `revision: str = "0005"` and `down_revision` set to `"0004"`
    - Migration creates four tables (grep for `create_table('plum_classification_tag'`, `'plum_part'`, `'plum_part_tag'`, `'plum_part_revision'`) and the partial index `uq_plum_part_one_released` with `postgresql_where`
    - `backend/app/modules/plum/seed.py` contains `async def seed_plum_data` and seeds six tag names + the two `plum.` setting keys
    - `backend/app/core/seed.py` run_seeds() calls `seed_plum_data` after `seed_gl_accounts`
    - Both new Python files parse without SyntaxError (the ast.parse in the verify command prints OK)
  </acceptance_criteria>
  <done>Alembic discovers the PLUM tables; migration 0005 chains onto 0004 with the partial unique index; the idempotent seed is wired into run_seeds().</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 backend test scaffold for PLUM-01/02/03</name>
  <files>backend/tests/plum/__init__.py, backend/tests/plum/test_parts.py, backend/tests/plum/test_revisions.py</files>
  <read_first>
    - backend/tests/syerp/test_partners.py (the test analog — client + skip_if_no_db fixtures, create_access_token token pattern, 201/409/422/200 assertions, the AuditLog-row verification pattern)
    - backend/tests/conftest.py (the client and skip_if_no_db fixtures these tests consume)
    - .planning/phases/05-plum-parts-revisions/05-RESEARCH.md (Phase Requirements → Test Map — the exact 14 test names and behaviors; Validation Architecture section)
    - .planning/phases/05-plum-parts-revisions/05-PATTERNS.md (test_parts.py and test_revisions.py Pattern Assignments — header docstrings, token permissions, audit verification)
  </read_first>
  <action>
    Create `backend/tests/plum/__init__.py` (empty package init) and two test modules mirroring `tests/syerp/test_partners.py` structure (async functions taking `client: httpx.AsyncClient` and `skip_if_no_db: None`, building a token via `create_access_token(subject="admin-user", permissions=["plum:write","plum:read"])`, asserting on `/api/v1/plum/...` responses).

    In `test_parts.py` (PLUM-01, PLUM-02), define these test functions covering the RESEARCH test map: test_create_part (201, part_number present, active True, first revision auto-created in Draft), test_create_duplicate_part_number (409), test_update_part (200 + AuditLog row written via the AsyncSessionLocal select pattern), test_archive_part (active=false, absent from default list), test_create_requires_write_permission (403 without plum:write), test_search_by_part_number, test_search_by_description, test_filter_by_status.

    In `test_revisions.py` (PLUM-03), define: test_create_revision (201, status draft, attributes copied forward), test_advance_to_in_review (200), test_release_supersedes_prior (release Rev B obsoletes Rev A; assert exactly one released row per part), test_released_revision_immutable (PATCH a released revision → 422), test_revision_history_order (newest-first ordering).

    Each test body MUST contain real assertions against the endpoints (not `pass`). These will FAIL/ERROR until 05-02 implements the service+router — that is expected Wave 0 behavior (RED). Keep them importable and collectable so pytest discovers them.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/plum/ --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/plum/test_parts.py` contains `def test_create_part`, `def test_create_duplicate_part_number`, `def test_update_part`, `def test_archive_part`, `def test_create_requires_write_permission`, `def test_search_by_part_number`, `def test_search_by_description`, `def test_filter_by_status`
    - `backend/tests/plum/test_revisions.py` contains `def test_create_revision`, `def test_advance_to_in_review`, `def test_release_supersedes_prior`, `def test_released_revision_immutable`, `def test_revision_history_order`
    - `cd backend && python -m pytest tests/plum/ --collect-only -q` collects ≥13 test items with exit code 0 (collection succeeds even though tests are RED)
    - Each test references `/api/v1/plum/` paths and uses `create_access_token` with `plum:` permissions
    - No test body is a bare `pass` (grep for `assert` in each file returns ≥1 per test): `grep -c "assert " backend/tests/plum/test_parts.py` → ≥8
  </acceptance_criteria>
  <done>pytest collects the full PLUM Wave 0 suite; the 13 tests encode the PLUM-01/02/03 behaviors and are ready to go GREEN in 05-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → live DB | Schema-defining DDL applied on startup; a wrong constraint corrupts data integrity for all later phases |
| Seed → settings/tag tables | Idempotency boundary; a non-idempotent seed double-inserts on every `podman-compose up` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | plum_part_revision "one Released per part" invariant | mitigate | Partial unique index `uq_plum_part_one_released ON (part_id) WHERE status='released'` enforced at DB level in migration 0005 (RESEARCH Pitfall 3) — defends the FSM even against concurrent release races |
| T-05-02 | Tampering | run_seeds() idempotency | mitigate | seed_plum_data uses select-before-insert on tag name and Setting key (never blind INSERT) — verified safe across repeated startups |
| T-05-03 | Tampering | Alembic autogenerate completeness | mitigate | core/models.py import uncommented so Base.metadata includes plum tables; migration hand-authored and chained to down_revision 0004 (RESEARCH Pitfall 5) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: none new) — supply-chain surface unchanged from Phases 1-4 |
</threat_model>

<verification>
- `cd backend && python -c "import app.core.models"` succeeds (Alembic discovery wired)
- `cd backend && python -m pytest tests/plum/ --collect-only -q` collects the Wave 0 suite
- Migration 0005 declares `down_revision = "0004"` and the partial unique index
- seed_plum_data wired into run_seeds() after seed_gl_accounts
</verification>

<success_criteria>
- Four plum_ tables defined in models + migration 0005 (D-01/D-02/D-12)
- Schemas (PartCreate/Update/Read, RevisionCreate/Read, PartDetailRead) defined as contracts for 05-02/03/04
- Idempotent seed for tag vocabulary + plum settings wired into startup
- DB-level one-Released-per-part invariant present
- Wave 0 backend tests collectable (13 tests across PLUM-01/02/03)
</success_criteria>

<output>
Create `.planning/phases/05-plum-parts-revisions/05-01-SUMMARY.md` when done.
</output>
