---
phase: 2
slug: authentication-users
status: draft
shadcn_initialized: true
preset: "style: default, baseColor: slate, cssVariables: true"
created: 2026-06-23
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Phase 2: Authentication & Users.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized) |
| Preset | default / slate base / cssVariables |
| Component library | Radix UI (via shadcn default style) |
| Icon library | lucide-react 1.21.0 (already in package.json) |
| Font | System stack — no custom font declared; Tailwind default sans applies |

**Source:** `frontend/components.json`, `frontend/package.json` (codebase scan)

---

## Screens in Scope

Phase 2 delivers exactly three frontend surfaces. The app shell / navigation is Phase 3.

| Screen | Route | Requirement |
|--------|-------|-------------|
| Login | `/login` | CORE-02, CORE-03 |
| Protected route loading guard | (layout, no URL) | CORE-03 |
| Admin — User Management | `/admin/users` | CORE-04, CORE-05 |

No open signup screen (D-01 locked: accounts are admin-provisioned; `signup_enabled` defaults to `false`).

---

## Spacing Scale

Standard 8-point scale. Pre-populated from template; consistent with Tailwind defaults and existing `Landing.tsx` patterns (`p-4`, `p-8`, `space-y-4`, `space-y-8`).

| Token | Value | Tailwind class | Usage |
|-------|-------|----------------|-------|
| xs | 4px | `p-1` / `gap-1` | Icon gaps, status dot margin, inline tight spacing |
| sm | 8px | `p-2` / `gap-2` | Compact element spacing, input icon inset |
| md | 16px | `p-4` / `gap-4` | Default card padding, form field spacing |
| lg | 24px | `p-6` / `gap-6` | Card-to-card spacing, section breaks within a page |
| xl | 32px | `p-8` / `gap-8` | Page-level outer padding (matches Landing) |
| 2xl | 48px | `py-12` | Login card vertical centering offset |
| 3xl | 64px | (not used this phase) | Reserved for later shell layout |

**Exceptions:**
- Touch targets for icon-only buttons (deactivate action): minimum 44px height via `h-11` or `min-h-[44px]` — WCAG 2.5.5.
- Table row height: 48px minimum (`h-12`) for comfortable click targets in the user list.

**Source:** 8-point default + `Landing.tsx` codebase patterns.

---

## Typography

Four type sizes already established by `Landing.tsx`. This phase uses the same four; no additions.

| Role | Size | Tailwind | Weight | Tailwind | Line Height | Usage |
|------|------|----------|--------|----------|-------------|-------|
| Body | 16px | `text-base` | 400 (regular) | `font-normal` | 1.5 | Form labels, table cell text, paragraph copy |
| Label | 14px | `text-sm` | 500 (medium) | `font-medium` | 1.4 | Field labels above inputs, status badges, table column headers |
| Heading | 20px | `text-xl` | 600 (semibold) | `font-semibold` | 1.2 | Page headings (`h1` on Login, `h2` on User Management) |
| Display | 36px | `text-4xl` | 700 (bold) | `font-bold` | 1.1 | App name on Login page only (matches Landing `h1`) |

**Weights used: 2** — regular (400) for body/muted text, semibold (600) for headings and labels. `font-bold` (700) reserved only for the Login display line.

**Mono exception:** Error messages from API (`error.message`) render in `font-mono text-xs` — matches Landing pattern for API error text.

**Source:** `frontend/src/routes/Landing.tsx` codebase patterns (text-4xl font-bold, text-lg, text-sm font-semibold, text-xs); standard shadcn type scale.

---

## Color

All values are the shadcn slate-base tokens already declared in `frontend/src/index.css`. Do not introduce hex values directly — always reference CSS variables.

| Role | CSS Variable | Resolved (light) | Usage |
|------|-------------|-----------------|-------|
| Dominant (60%) | `hsl(var(--background))` | #ffffff | Page background, login page fill |
| Secondary (30%) | `hsl(var(--card))` | #ffffff / `--secondary: hsl(210 40% 96.1%)` | Login card, user table container, form section backgrounds |
| Accent (10%) | `hsl(var(--primary))` | #0f172a (slate-900) | Primary CTA button ("Sign In", "Save Changes") only |
| Destructive | `hsl(var(--destructive))` | #ef4444 | Deactivate user button + confirmation dialog trigger only |

**Accent reserved for:**
1. The primary CTA button on the Login page ("Sign In")
2. The "Save Changes" button in the Edit User drawer
3. The "Create User" button on the User Management page

Accent is NOT used on table row hover, links, icons, badges, or navigation items.

**Semantic status colors** (not in the 60/30/10 palette — used inline for data states only):
- Active user badge: `text-green-600` / `bg-green-50` (matches Landing health indicator)
- Inactive/deactivated user badge: `text-gray-500` / `bg-gray-100`
- Error inline text: `text-red-600` (matches Landing error pattern)

**Source:** `frontend/src/index.css` shadcn slate token set; `frontend/src/routes/Landing.tsx` status color patterns.

---

## Component Inventory

Shadcn components to install for this phase. All from official shadcn registry — no third-party registries.

| Component | Command | Screen |
|-----------|---------|--------|
| Button | `npx shadcn add button` | Login CTA, User Management actions |
| Input | `npx shadcn add input` | Email field, Password field, Search/filter field |
| Label | `npx shadcn add label` | Form field labels |
| Card | `npx shadcn add card` | Login card container, user detail panel |
| Table | `npx shadcn add table` | User list (CORE-04) |
| Badge | `npx shadcn add badge` | Active/inactive user status, role label |
| Dialog | `npx shadcn add dialog` | Deactivate user confirmation (destructive) |
| Sheet | `npx shadcn add sheet` | Create / Edit user drawer (CORE-04) |
| Select | `npx shadcn add select` | Role assignment dropdown (CORE-05) |
| Separator | `npx shadcn add separator` | Section dividers in the Edit user sheet |

No third-party registry blocks declared. Registry safety gate: not applicable.

---

## Screen Contracts

### Screen 1: Login (`/login`)

**Layout:** Centered card on full-height background. Max width 400px. Vertically centered.

```
[ BizNiceSweets ]           ← Display (text-4xl font-bold)
[ Modular business suite ]  ← Body muted (text-base text-muted-foreground)

[ Card ]
  Email                     ← Label (text-sm font-medium)
  [ ________________________ ]  ← Input
  Password                  ← Label
  [ ________________________ ]  ← Input (type="password")
  [ Sign In ]               ← Button variant="default" (accent, full width)

[ inline error if needed ]  ← text-sm text-red-600 below the button
```

**States:**
- **Default:** Both inputs empty. Button enabled.
- **Loading:** Button shows spinner icon (lucide `Loader2` with `animate-spin`), text becomes "Signing in…", button disabled.
- **Error:** Inline error text below button. Input borders highlight (`border-destructive` via shadcn). No toast.
- **Success:** Redirect to protected app shell (Phase 3 placeholder route `/`).

No "Create account" link (D-01). No "Forgot password" link (D-13: admin-reset-first; email recovery is SMTP-gated and not a Phase 2 UI element).

---

### Screen 2: Protected Route Guard (layout component)

Not a visible screen. Renders between route load and auth resolution.

**Loading state:** Full-screen centered spinner only — `Loader2` icon, `h-8 w-8 animate-spin text-muted-foreground`, no text.

**Unauthenticated redirect:** No visible flash — `<Navigate to="/login" replace />` fires before first paint (TanStack Query `isLoading` guard).

---

### Screen 3: Admin — User Management (`/admin/users`)

**Layout:** Full-width content area. Page heading row + toolbar row + table. No sidebar (Phase 3 adds navigation shell).

```
Users                                     ← Heading (text-xl font-semibold)
Manage who can access this installation.  ← Body muted

[ Search users... ]  [ Create User ]      ← Input + Button (toolbar row)

┌─────────────────────────────────────────────────────┐
│ Name       Email            Role     Status  Actions │
│ ───────────────────────────────────────────────────  │
│ Jane Doe   jane@co.com      admin    Active  ⋮       │
│ Bob Smith  bob@co.com       user     Inactive ⋮      │
└─────────────────────────────────────────────────────┘
```

**Table columns:** Full Name | Email | Role(s) | Status | Actions (overflow menu)

**Actions overflow menu (lucide `MoreHorizontal`):**
- "Edit" → opens Edit User Sheet
- "Deactivate" → opens Deactivate confirmation Dialog (destructive)
- "Activate" (shown only when user is inactive) → direct action, no confirmation

**Create / Edit User Sheet:**
- Slide-in from right (`Sheet` component, side="right")
- Fields: Full Name, Email, Password (create only — edit uses admin-reset), Role (Select dropdown)
- Footer: Cancel | Save Changes (accent button)
- Password field on Create: show/hide toggle (lucide `Eye`/`EyeOff` icon button, sm touch target 32px — acceptable for non-primary action)

**Table empty state:** Shown when no users match the search filter (or on fresh install before seed runs — seed creates admin automatically, so fresh-install empty state is unlikely but must exist).

**Role badge:** Each role renders as a `Badge` variant="outline" (e.g. "admin", "user"). Multiple badges if user has multiple roles.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Login page heading | "BizNiceSweets" |
| Login page subheading | "Sign in to your account" |
| Login primary CTA | "Sign In" |
| Login loading CTA | "Signing in…" |
| Login error — bad credentials | "Incorrect email or password. Check your credentials and try again." |
| Login error — server/network | "Unable to reach the server. Check that the backend is running." |
| User Management page heading | "Users" |
| User Management page description | "Manage who can access this installation." |
| Create User button | "Create User" |
| Search placeholder | "Search users…" |
| Empty state heading (no users found) | "No users found" |
| Empty state body | "No users match your search. Clear the filter or create a new user." |
| Edit sheet title | "Edit User" |
| Create sheet title | "Create User" |
| Save CTA | "Save Changes" |
| Deactivate action label | "Deactivate" |
| Activate action label | "Activate" |
| Deactivate confirmation heading | "Deactivate user?" |
| Deactivate confirmation body | "This will immediately end all active sessions for {full_name}. They will not be able to log in until reactivated." |
| Deactivate confirmation CTA | "Deactivate" (Button variant="destructive") |
| Deactivate cancel | "Cancel" |
| Protected route loading | (no text — spinner only) |
| Inactive user badge | "Inactive" |
| Active user badge | "Active" |

**Destructive action pattern:** Dialog with two-button footer (Cancel / Deactivate). No inline `confirm()`. The deactivate button uses shadcn `Button variant="destructive"`. No "type the name to confirm" friction — this is an admin internal tool, not a user self-serve danger zone.

**Source:** CONTEXT.md D-01, D-04, D-05, D-13; CORE-04/CORE-05 requirements; standard shadcn admin patterns.

---

## Interaction Contracts

| Interaction | Behavior |
|-------------|----------|
| Login form submit | `Enter` key in either field submits. Button click submits. Disable button during in-flight request. |
| Password field visibility toggle | Toggle button adjacent to input (lucide Eye/EyeOff). Toggles `type="password"` ↔ `type="text"`. |
| User table search | Client-side filter on name+email as user types (debounce 300ms). No server round-trip for search. |
| Create User sheet open | Sheet slides in from right. Focus moves to first input (Full Name). |
| Save Changes | Calls API, shows loading state on button, closes sheet on success, invalidates TanStack Query `['users']` cache. |
| Deactivate confirmation | Dialog opens. Focus traps inside. ESC or Cancel closes without action. Deactivate button calls API, closes dialog on success. |
| Session expiry (mid-session) | Silent refresh interceptor fires on 401. If refresh fails, redirect to `/login` with no user-visible error (they see the login page). |
| Role assignment | Single-select `Select` dropdown (Phase 2 seed has two roles: admin + user). Multiple role assignment deferred until role management UI is built. |

---

## Accessibility

- All form inputs have associated `<label>` via shadcn `Label` (htmlFor pairing).
- Error messages are associated to their input via `aria-describedby`.
- Dialog has `aria-labelledby` pointing to heading, focus trapped inside.
- Sheet has `aria-labelledby` pointing to Sheet title.
- Destructive button has `aria-label="Deactivate {full_name}"` for screen reader context.
- Minimum touch target 44px for all primary actions (login button full-width, table action triggers).
- Color is never the only differentiator — Active/Inactive states use both badge color AND text label.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, input, label, card, table, badge, dialog, sheet, select, separator | not required — official registry |

No third-party registries declared. Gate: not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
