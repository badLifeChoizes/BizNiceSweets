---
phase: 07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/modules/plum/service.py
  - backend/tests/plum/test_import_export.py
  - backend/tests/plum/test_parts.py
autonomous: true
requirements: [PLUM-07, PLUM-10, PLUM-01, PLUM-08]
must_haves:
  truths:
    - "POST /api/v1/plum/parts/{id}/avl returns 2xx (not 500 ImportError) for a valid is_vendor Partner"
    - "Import validate/commit and JSON/Excel export with vendor references succeed (not 500)"
    - "Auto-generated part numbers are numerically correct past the 5-digit→6-digit boundary and never collide with an existing row"
  artifacts:
    - path: "backend/app/modules/plum/service.py"
      provides: "Partner-aliased vendor lookups + numeric-safe part-number generation"
      contains: "Partner as SyerpPartner"
    - path: "backend/tests/plum/test_parts.py"
      provides: "Digit-boundary regression test for generate_part_number"
      contains: "test_generate_part_number_digit_boundary"
    - path: "backend/tests/plum/test_import_export.py"
      provides: "Export/commit coverage that exercises the vendor cross-reference code paths"
      contains: "avl_links"
  key_links:
    - from: "backend/app/modules/plum/service.py add_avl_link"
      to: "syerp_partner (Partner.is_vendor == True)"
      via: "aliased import + preserved WHERE filter"
      pattern: "Partner as SyerpPartner"
    - from: "backend/app/modules/plum/service.py generate_part_number"
      to: "numeric ORDER BY over ^P[0-9]+$ rows"
      via: "cast(func.substring(...), Integer)"
      pattern: "cast\\(func.substring"
---

<objective>
Fix the two backend defects rooted in `backend/app/modules/plum/service.py` that the v1.0 milestone audit reproduced live:

1. `SyerpPartner` class-name ImportError at 4 call sites (real class is `Partner`) — severs the entire PLUM→SYERP AVL/import/export cross-module dependency at runtime (500s).
2. `generate_part_number()` uses lexicographic `func.max()` on a VARCHAR, handing back a stale/duplicate number past a digit-width boundary (deterministic duplicate-key 500 on the next create).

Also close the two Wave-0 test gaps so the fixes are proven against a live DB (the existing tests were written but never run against Postgres, which is how these bugs shipped).

Purpose: Restore PLUM-07 (AVL vendor links), PLUM-10 (import/export vendor cross-ref), PLUM-08 (vendor-price cost source, transitively), and PLUM-01 reliable auto-numbering.
Output: A corrected `service.py`, an extended `test_import_export.py`, a new digit-boundary test in `test_parts.py`, all green against the live DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-RESEARCH.md
@.planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-PATTERNS.md

<interfaces>
<!-- The real class the fix must target. Extracted from codebase — do not modify syerp/models.py. -->

From backend/app/modules/syerp/models.py:39:
```python
class Partner(Base):
    __tablename__ = "syerp_partner"
    id: Mapped[str]          # String(36) PK
    code: Mapped[str]        # String(20) unique
    name: Mapped[str]
    is_vendor: Mapped[bool]
    is_customer: Mapped[bool]
    active: Mapped[bool]
```

Current broken import (identical at service.py lines 1634, 2139, 2607, 2740):
`from app.modules.syerp.models import SyerpPartner`   # class does not exist

Current sqlalchemy import (service.py line 66):
`from sqlalchemy import func, or_, select`

Live-DB test execution (Pitfall 1 — host pytest silently skips skip_if_no_db):
`podman exec compose_api_1 pytest tests/plum/... -x`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alias Partner as SyerpPartner at all 4 sites + cover every vendor code path live</name>
  <files>backend/app/modules/plum/service.py, backend/tests/plum/test_import_export.py</files>
  <read_first>
    - backend/app/modules/plum/service.py (lines 1630-1650, 2135-2150, 2600-2650, 2735-2775 — the 4 broken import sites and their WHERE clauses)
    - backend/app/modules/syerp/models.py (lines 39-67 — confirm class Partner and the is_vendor column)
    - backend/tests/plum/test_avl.py (test_add_avl_link, lines 26-76 — existing regression, run only)
    - backend/tests/plum/test_import_export.py (test_export_json ~36, test_import_preview_unknown_vendor ~151, test_import_commit* — fixture/seed style; note test_export_json currently seeds NO AVL row)
    - backend/tests/conftest.py (db_available/skip_if_no_db fixtures, lines 71-105)
  </read_first>
  <action>
    In `backend/app/modules/plum/service.py`, at each of the 4 function-local import statements (lines 1634 in add_avl_link, 2139 in build_json_export, 2607 in validate_import, 2740 in commit_import), replace `from app.modules.syerp.models import SyerpPartner` with `from app.modules.syerp.models import Partner as SyerpPartner`. This is an alias-only change — do NOT touch any other line in those functions, do NOT hoist the import to module top (function-local imports are the deliberate convention here to avoid plum↔syerp circular imports), and do NOT alter the `SyerpPartner.is_vendor == True` / `.is_vendor.is_(True)` WHERE filters (preserving T-06-07: AVL lookups must remain restricted to vendor Partners only). Do NOT modify `backend/app/modules/syerp/models.py` — the `Partner` class is correct.

    Then close the export/commit coverage gap so all 4 sites are exercised by an automated test that actually runs the broken code path: in `backend/tests/plum/test_import_export.py`, ensure at least one test seeds a `PlumAvlLink` row (so `build_json_export` at 2139 executes the vendor lookup rather than the empty-`vendor_ids` short-circuit) and that an import-commit case includes a valid existing vendor `code` in its `avl_links` payload (so `commit_import` at 2740 executes). Follow the file's existing fixture/seed conventions (`create_access_token`, `client`, direct API POST to seed) — sites 1634 and 2607 are already covered by test_avl.py::test_add_avl_link and test_import_export.py::test_import_preview_unknown_vendor respectively.
  </action>
  <verify>
    <automated>podman exec compose_api_1 alembic current && podman exec compose_api_1 pytest tests/plum/test_avl.py tests/plum/test_import_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Partner as SyerpPartner" backend/app/modules/plum/service.py` returns 4
    - `grep -c "import SyerpPartner$" backend/app/modules/plum/service.py` returns 0 (no bare broken import remains)
    - `backend/app/modules/syerp/models.py` is unchanged (git diff shows no edits)
    - `podman exec compose_api_1 alembic current` reports revision 0006 (confirms live DB reachable, per Pitfall 1)
    - `podman exec compose_api_1 pytest tests/plum/test_avl.py::test_add_avl_link -x` exits 0 (not skipped, not 500)
    - `podman exec compose_api_1 pytest tests/plum/test_import_export.py -x` exits 0 with 0 unexpected skips on the vendor-path cases
    - test_import_export.py contains a test that seeds a PlumAvlLink row before calling GET /plum/export/json (site 2139 exercised)
    - The `is_vendor` filter still appears at all 4 sites (`grep -c "is_vendor" backend/app/modules/plum/service.py` unchanged from before the edit)
  </acceptance_criteria>
  <done>All 4 vendor code paths import Partner correctly; AVL add, import validate/commit, and JSON export with vendor rows run against the live DB with exit 0 and no ImportError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Numeric-safe generate_part_number + digit-boundary regression test</name>
  <files>backend/app/modules/plum/service.py, backend/tests/plum/test_parts.py</files>
  <behavior>
    - Given DB rows P99999 and P100000 (both valid explicit overrides), generate_part_number() returns the numeric max + 1 formatted as P#####, i.e. it treats "P100000" > "P99999" (numeric), never returning a lexicographically-stale value.
    - The returned part_number does not duplicate any existing row (no unique-constraint 500 on the subsequent create).
    - A non-numeric-suffix row such as "P-DUPE-01" in the table does NOT cause a Postgres integer-cast error (it is excluded from the MAX computation by the regex filter).
    - Empty table (no ^P[0-9]+$ rows) still returns "P00001".
  </behavior>
  <read_first>
    - backend/app/modules/plum/service.py (lines 108-136 — current generate_part_number; line 66 — the sqlalchemy import to widen)
    - backend/tests/plum/test_parts.py (test_create_duplicate_part_number lines 57-82 for the explicit-part_number seed style; test_update_part lines 85-125 for direct-session assertion style; header lines 25-42 for fixtures/token)
    - .planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-PATTERNS.md (the generate_part_number fix pattern + Pitfall 3 note)
  </read_first>
  <action>
    Widen the sqlalchemy import at `service.py` line 66 to `from sqlalchemy import Integer, cast, func, or_, select`. Rewrite the query inside `generate_part_number()` (lines 108-136) so it selects `PlumPart.part_number`, filters to strictly numeric-suffixed rows with the Postgres POSIX regex operator `PlumPart.part_number.op("~")(r"^P[0-9]+$")`, orders by `cast(func.substring(PlumPart.part_number, 2), Integer).desc()`, and limits to 1 — then reads `result.scalar()` as `max_pn`. The regex filter MUST run before the cast so that non-numeric part_numbers like "P-DUPE-01" (real live data) are excluded and never reach the integer cast (Pitfall 3 — a bare cast over all `LIKE 'P%'` rows throws `invalid input syntax for type integer`). Keep the function signature (`async def generate_part_number(db: AsyncSession) -> str`), the `if max_pn is None: return "P00001"` branch, the existing `try/except (IndexError, ValueError): suffix = 0` defense-in-depth, and the `f"P{suffix + 1:05d}"` return format all unchanged — only the SELECT that computes `max_pn` changes. The regex pattern is a fixed literal, never request-derived (no injection/ReDoS surface).

    Add `test_generate_part_number_digit_boundary` to `backend/tests/plum/test_parts.py` following the file's conventions (`skip_if_no_db`, `create_access_token(subject="admin-user", permissions=["plum:write"])`, `client`). Seed `P99999` and `P100000` via explicit-part_number POSTs, then POST a part with no part_number to trigger auto-generation. Because the shared live dev DB already contains rows past this boundary (RESEARCH Runtime State Inventory), assert robustly rather than on a hardcoded string: the returned part_number matches `^P[0-9]+$`, its numeric suffix is strictly greater than the numeric suffix of every pre-existing `^P[0-9]+$` row (i.e. it equals the true numeric MAX + 1), and it is not equal to any existing row's part_number (no duplicate). Include an inline comment referencing the shared-DB caveat.
  </action>
  <verify>
    <automated>podman exec compose_api_1 pytest tests/plum/test_parts.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from sqlalchemy import Integer, cast, func, or_, select" backend/app/modules/plum/service.py` returns 1
    - `service.py` generate_part_number contains `op("~")(r"^P[0-9]+$")` and `cast(func.substring` (grep both present)
    - `service.py` generate_part_number still returns via `f"P{suffix + 1:05d}"` and still has the `return "P00001"` empty-table branch
    - `backend/tests/plum/test_parts.py` contains `def test_generate_part_number_digit_boundary`
    - `podman exec compose_api_1 pytest tests/plum/test_parts.py::test_generate_part_number_digit_boundary -x` exits 0 (test runs, not skipped)
    - `podman exec compose_api_1 pytest tests/plum/test_parts.py -x` exits 0 (no regression on existing part tests, including test_create_duplicate_part_number which uses "P-DUPE-01")
  </acceptance_criteria>
  <done>generate_part_number returns the numeric successor past the digit-width boundary, excludes non-numeric part_numbers from the cast, and the new live-DB regression test passes alongside the existing part-number tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (`/plum/parts/{id}/avl`, `/plum/import/*`, `/plum/export/*`) | Untrusted request bodies cross here; already RBAC-gated by `require_permission` (Phase 2/5/6). Not modified by this plan. |
| service→DB (Postgres) | Query construction crosses here; the part-number regex/cast is the only new query shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege / Tampering | `add_avl_link` + 3 import/export sites | mitigate | Fix is import-alias only; the `SyerpPartner.is_vendor == True` WHERE filter is preserved verbatim at all 4 sites so AVL lookups cannot be widened to non-vendor Partners (upholds T-06-07). Acceptance criteria assert is_vendor count unchanged. |
| T-07-02 | Denial of Service | `generate_part_number` regex/cast query | mitigate | `^P[0-9]+$` filter runs before the integer cast so malformed part_numbers (e.g. "P-DUPE-01") never reach the cast and cannot trigger a DataError-500. Regex is a fixed string literal (no request-derived input → no injection/ReDoS). |
| T-07-03 | Tampering (SQL injection) | `.op("~")` regex operator | accept | Pattern is a hardcoded Python literal passed through SQLAlchemy's parameterized construct, never string-interpolated user input — injection risk is nil. |

No package-manager installs occur in this plan (no new dependencies) — no supply-chain (T-07-SC) checkpoint required.
</threat_model>

<verification>
- `podman exec compose_api_1 alembic current` → 0006 (live DB reachable before any test claims to pass — guards against the Pitfall-1 silent-skip that let these bugs ship)
- `podman exec compose_api_1 pytest tests/plum/ -q` → green with 0 unexpected skips on test_avl.py, test_import_export.py, test_parts.py
- No edits to `backend/app/modules/syerp/models.py`
</verification>

<success_criteria>
- All 4 `SyerpPartner` sites import `Partner as SyerpPartner`; AVL add and import/export vendor paths return 2xx (not 500) live.
- `generate_part_number` returns the correct numeric successor past a digit-width boundary and excludes non-numeric part_numbers from the cast.
- The new digit-boundary test and the extended export/commit coverage run (not skip) against the live DB and pass.
</success_criteria>

<output>
Create `.planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-01-SUMMARY.md` when done.
</output>
