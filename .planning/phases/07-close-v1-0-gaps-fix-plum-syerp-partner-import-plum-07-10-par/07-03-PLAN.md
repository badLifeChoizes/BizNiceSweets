---
phase: 07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified: []
autonomous: false
requirements: [PLUM-04, PLUM-05, PLUM-06, PLUM-07, PLUM-08, PLUM-09, PLUM-10]
must_haves:
  truths:
    - "User can add a child part to a parent, view it in an expandable BOM tree, and a shared sub-assembly rolls up to ONE flat-view row with summed quantity"
    - "User can run where-used and see direct/indirect parent assemblies"
    - "User can add a vendor link (AVL) that persists — with no 500 error"
    - "User can set material cost / vendor price and see effective cost + roll-up + margin, with negative margin shown in red"
    - "User can export JSON and Excel, re-import JSON with 0 errors, confirm, and the Parts List reflects the import with no manual refresh"
    - "A Released revision is read-only (no Add Part, no cost edit form) and shows the frozen 'Released at' cost"
    - "Creating a new part past the seeded high part_number succeeds with no duplicate-key error"
  artifacts:
    - path: ".planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-03-SUMMARY.md"
      provides: "Recorded human-verify outcome for the 7 PLUM flows + 4 regression checks (satisfies the unrun 06-05 Task 4 checkpoint)"
  key_links:
    - from: "human verification"
      to: "PLUM-04..10 success criteria"
      via: "single consolidated UAT pass against the live Podman stack"
      pattern: "N/A (manual)"
---

<objective>
Run ONE consolidated human-verify pass that satisfies both the never-run Phase-6 `06-05` Task 4 checkpoint (7 ROADMAP success criteria + Released immutability) AND the 4 Phase-7 regression checks that prove the Plan 07-01 / 07-02 fixes landed. Per RESEARCH Pitfall 4, do not schedule two separate manual passes — this is the superset.

Purpose: Convert PLUM-04..10 from "code-complete, unverified" to human-verified, and confirm the two runtime blockers (AVL/import 500s) and the cache-staleness gap are gone in the actual UI.
Output: A recorded pass/fail outcome per flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-plum-bom-costing-integration/06-05-SUMMARY.md
@.planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Consolidated Phase-6 + Phase-7 UI verification</name>
  <read_first>
    - .planning/phases/06-plum-bom-costing-integration/06-05-SUMMARY.md (lines 126-138 — the 8-item Phase-6 verify list this checkpoint supersedes)
    - .planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-RESEARCH.md (Pitfall 2 & 4 — why unit-test-green is not sufficient for PLUM-07/10)
  </read_first>
  <action>
    Present the how-to-verify checklist below to the user and pause for their sign-off (blocking checkpoint — no code is written in this task). First confirm the environment is live and migrated (`podman ps` + `podman exec compose_api_1 alembic current` → 0006), then have the user exercise flows 1-12 in the running UI and report the outcome. Record each flow's pass/fail plus any observed detail in 07-03-SUMMARY.md. Any failure routes to `/gsd:plan-phase --gaps`.
  </action>
  <what-built>
    Plan 07-01 fixed the backend `SyerpPartner`→`Partner` ImportError (AVL add + import/export vendor cross-ref) and the lexicographic part-number generator; Plan 07-02 added the missing Parts-List cache invalidation after import commit. Automated live-DB tests are green. This checkpoint confirms the fixes in the actual running UI and clears the unverified Phase-6 flows.
  </what-built>
  <how-to-verify>
    Precondition: confirm the stack is up and migrated — `podman ps` shows compose_db_1 / compose_api_1 / compose_frontend_1 running, and `podman exec compose_api_1 alembic current` reports 0006. Open the frontend (Vite dev, http://localhost:5173, or the served SPA). Log in as admin.

    Phase-6 success criteria (from 06-05-SUMMARY):
    1. BOM card — on a Draft revision, Add Part → the child appears in the tree; expand/collapse works.
    2. BOM flat view — a shared sub-assembly appears as ONE row with summed Total Qty and a Total BOM Cost footer.
    3. Where Used card — parent assemblies appear, labeled "Direct parent" / "Indirect via {part}".
    4. AVL card — Add Vendor → select a SYERP vendor → link PERSISTS after refresh; mark Preferred shows the badge. (REGRESSION: this must NOT 500 — it was the PLUM-07 blocker.)
    5. Cost & Margin — enter Material Cost, Save → Effective Cost shows source "manual"; on an assembly the source shows "roll-up"; select-for-costing on an AVL price break shows source "vendor price".
    6. Sale Price → Margin and Margin % render; a below-cost sale price shows the margin in red.
    7. Import/Export — Export as JSON and Export as Excel both download; re-import the JSON → preview shows 0 errors → Confirm Import → success state; "No records were deleted"; a >10 MB file is rejected. (REGRESSION: import/export with vendor references must NOT 500 — PLUM-10 blocker.)
    8. Released revision — BOM and cost are read-only (no Add Part button, no cost edit form) and the frozen "Released at" cost is shown.

    Phase-7-specific regression checks:
    9. AVL add (criterion 4) completes with NO 500 / error toast.
    10. Import containing a vendor reference (criterion 7) previews and commits with NO 500.
    11. After Confirm Import, the Parts List reflects the imported/updated rows WITHOUT a manual page refresh (cache-invalidation fix).
    12. Create a NEW part with auto-numbering (no part_number entered) on the current instance (which already has data past the P99999→P100000 boundary) → it succeeds with a fresh unique P##### and NO duplicate-key error.
  </how-to-verify>
  <acceptance_criteria>
    - All 8 Phase-6 criteria observed working, OR any failure recorded with specifics for gap-closure.
    - Regression checks 9-12 explicitly confirmed: no 500 on AVL add, no 500 on vendor-ref import, Parts List auto-refreshes after commit, auto-part-number create succeeds with no collision.
    - Outcome per flow (pass/fail + notes) recorded in 07-03-SUMMARY.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all flows pass, or list the failing flow(s) with what you observed so the gaps can be planned via `/gsd:plan-phase --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
This plan writes no code and adds no attack surface — it is a manual verification checkpoint against the already-deployed, already-RBAC-gated stack. No STRIDE threats apply beyond those already dispositioned in Plans 07-01/07-02. No package installs.
</threat_model>

<verification>
- `podman ps` + `podman exec compose_api_1 alembic current` (0006) before verifying — guards against verifying a stale/unmigrated environment (the audit noted a container that sat at schema 0005).
- Human confirms flows 1-12; failures route to `/gsd:plan-phase --gaps`.
</verification>

<success_criteria>
- All 7 PLUM flows (PLUM-04..10) confirmed working in the live UI.
- The two former runtime blockers (AVL/import 500s) and the cache-staleness gap are confirmed resolved in the UI.
- A new part created past the digit-width boundary succeeds.
</success_criteria>

<output>
Create `.planning/phases/07-close-v1-0-gaps-fix-plum-syerp-partner-import-plum-07-10-par/07-03-SUMMARY.md` recording the per-flow outcome.
</output>
